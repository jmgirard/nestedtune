# the refusals read the same for both accessors

    Code
      extract_tune_results(1:3)
    Condition
      Error in `extract_tune_results()`:
      ! `extract_tune_results()` has no method for an integer vector.
      i It answers for a <nested_final_fit> object, from `nested_final_fit()`.
    Code
      extract_scored_candidates(1:3)
    Condition
      Error in `extract_scored_candidates()`:
      ! `extract_scored_candidates()` has no method for an integer vector.
      i It answers for a <nested_final_fit> object, from `nested_final_fit()`.

