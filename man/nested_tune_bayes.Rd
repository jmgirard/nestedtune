% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested-tune-bayes.R
\name{nested_tune_bayes}
\alias{nested_tune_bayes}
\title{Run the nested cross-validation loop with Bayesian optimization inside}
\usage{
nested_tune_bayes(
  object,
  resamples,
  ...,
  iter = 10,
  param_info = NULL,
  metrics = NULL,
  initial = 5,
  objective = tune::exp_improve(),
  event_level = "first",
  eval_time = NULL
)
}
\arguments{
\item{object}{A \code{\link[workflows:workflow]{workflows::workflow()}} with at least one parameter marked
for tuning with \code{\link[tune:tune]{tune::tune()}}.}

\item{resamples}{A nested resampling design, from \code{\link[=nested_resamples]{nested_resamples()}} or
\code{\link[rsample:nested_cv]{rsample::nested_cv()}}. Its \code{splits} column must hold \code{rsplit} objects and
its \code{inner_resamples} column an \code{rset} per outer fold. Both are checked
before anything is fitted, because \code{\link[rsample:nested_cv]{rsample::nested_cv()}} builds a design
whatever its \code{inside} argument returned — so a specification that produces
no \code{rset} gives a design that cannot be run, where \code{\link[=nested_resamples]{nested_resamples()}}
refuses one at construction.}

\item{...}{Not used; must be empty. Everything after it is matched by name,
so a mistyped or unsupported argument is an error rather than a silent
positional match.}

\item{iter}{The maximum number of search iterations, passed to
\code{\link[tune:tune_bayes]{tune::tune_bayes()}}. A single non-negative whole number. Each iteration
proposes one candidate and scores it on the fold's inner resamples. \code{0}
scores the initial candidates and proposes nothing, which makes the run
the same as \code{\link[=nested_tune_grid]{nested_tune_grid()}} on the space-filling grid those
candidates form. tune stops a fold's search early when no unscored
candidate remains, saying so on the console and keeping what it has; the
fold completes with the candidates scored so far, and nothing about the
early stop reaches \code{.notes}.}

\item{param_info}{A \code{\link[dials:parameters]{dials::parameters()}} object, or \code{NULL} to let tune
derive one from the workflow. Passed unchanged to \code{\link[tune:tune_bayes]{tune::tune_bayes()}} on
every outer fold: the initial candidates are drawn from its ranges, and
every proposal stays inside them.}

\item{metrics}{A \code{\link[yardstick:metric_set]{yardstick::metric_set()}}, or \code{NULL} to use tune's defaults
for the model's mode. The first metric in the set selects the best inner
candidate.}

\item{initial}{The number of candidates to score before the first
iteration: a single whole number of at least 2. Each fold generates its own
space-filling set of that size from the parameter ranges with
\code{\link[dials:grid_space_filling]{dials::grid_space_filling()}} and scores it with \code{\link[tune:tune_grid]{tune::tune_grid()}}, under
the fold's own tuning seed. A \code{tune_results} object, which
\code{\link[tune:tune_bayes]{tune::tune_bayes()}} also accepts here, is refused: one tuning run cannot
serve every outer fold, and its candidates were scored on resamples that
may hold a fold's assessment rows.}

\item{objective}{An acquisition function from tune, deciding which candidate
the Gaussian process proposes next: \code{\link[tune:exp_improve]{tune::exp_improve()}} (the default),
\code{\link[tune:prob_improve]{tune::prob_improve()}} or \code{\link[tune:conf_bound]{tune::conf_bound()}}.}

\item{event_level}{\code{"first"} (the default) or \code{"second"}, naming which level
of a two-class outcome factor is the event. It reaches both loops: the
inner tuning run, where it decides which candidate is selected, and the
outer scoring fit, where it decides what the reported metrics mean.
Metrics that do not distinguish the two levels -- accuracy, \code{roc_auc},
\code{brier_class} -- are unaffected by it; \code{sens}, \code{spec}, \code{precision} and
their relatives are not. Ignored for a regression model, as it is in tune.}

\item{eval_time}{A numeric vector of evaluation times for a censored
regression model, or \code{NULL} (the default) to leave the choice to tune. It
reaches every tune call whose answer depends on it, so a dynamic or
integrated survival metric -- \code{brier_survival()}, \code{roc_auc_survival()} and
their relatives -- is measured at the times you name. It is ignored, with
a warning from tune, whenever the metric set has no metric that reads it.
tune keys that warning on the metrics rather than on the model's mode: a
set with no survival metric draws one saying the argument is only used
for censored regression, and a censored regression model scored only by a
static metric such as \code{concordance_survival()} draws a different one,
saying it is only used for dynamic or integrated survival metrics.

Refused here, ahead of tune: anything that is not numeric, an empty
vector, and any element that is missing, negative or not finite. tune
treats those unevenly, and only once a metric reads the times -- a
character value that reads as a number, such as \code{"1"}, is coerced with
\code{as.numeric()} and accepted, one that does not becomes missing; a
missing, negative or infinite element is dropped with a warning; and an
empty vector, or one that dropping has emptied, aborts -- and this package
refuses them all at entry, before a whole run is paid for. Zero, repeated
times and times out of order are accepted and passed on untouched, since
tune normalizes those itself; a repeated time draws tune's warning that 0
inappropriate evaluation time points were removed, once per tune call.}
}
\value{
An object of class \code{nested_results}, one row per outer fold, with
the columns \code{\link[=nested_tune_grid]{nested_tune_grid()}} documents. Two things differ from a grid
run.

Each fold's \code{.grid} -- the candidates its inner search actually scored --
carries an \code{.iter} column beside the parameter columns and \code{.config}: \code{0}
for the initial candidates and \code{i} for the candidate the \code{i}-th iteration
proposed. Every candidate that scored on at least one inner resample is a
row, so a fold that stopped early holds the rows it reached. As on the
grid path, a candidate that failed on every inner resample is absent, and
its failure is in \code{.notes}.

There is no \code{grid} attribute: \code{attr(x, "grid")} is \code{NULL}, because nothing
was asked for as a grid. What was asked for is on the \code{procedure}
attribute, which every result of either orchestrator carries: a named list
giving the tuner (\code{"tune_bayes"} here, \code{"tune_grid"} there), that tuner's
own arguments (\code{iter}, \code{initial} and \code{objective} here, \code{grid} there), and
\code{param_info}, \code{event_level} and \code{eval_time} on both. \code{attr(x, "metrics")}
holds the \code{metrics} argument as on the grid path, absent when none was
given. The record describes the call, so it travels with the class through
every dplyr and vctrs door the grid path's help page describes, and is
shed with the class by the operations that shed it.
}
\description{
\code{nested_tune_bayes()} drives the outer loop of nested cross-validation with
\code{\link[tune:tune_bayes]{tune::tune_bayes()}} as the inner tuner. For each outer fold it scores an
initial set of candidates on that fold's inner resamples, lets a Gaussian
process propose the next \code{iter} candidates one at a time, selects the best,
finalizes the workflow, and fits and scores it on the outer split with
\code{\link[tune:last_fit]{tune::last_fit()}}. It is \code{\link[=nested_tune_grid]{nested_tune_grid()}} with the inner tuner swapped:
the loop, the seeds, the results object and its methods are the same, and
that function's help page is the reference for everything the two share --
what a failed fold records, how the folds run in parallel, and what an
operation on the result may do.
}
\details{
The estimate this returns describes the whole search-and-fit \emph{procedure},
not any single fitted model, exactly as for \code{\link[=nested_tune_grid]{nested_tune_grid()}}; report it
for that procedure.
}
\section{Reproducibility}{


The seed contract is \code{\link[=nested_tune_grid]{nested_tune_grid()}}'s: seed the session before the
call, there is no \code{seed} argument, \code{2 * n} seeds are drawn in one
\code{sample.int(.Machine$integer.max, 2 * n)} call on entry, and fold \code{i} tunes
under element \code{2 * i - 1} and fits under element \code{2 * i}, each applied with
the generator kind pinned.

One rule is this function's own. \code{\link[tune:control_bayes]{tune::control_bayes()}} has a \code{seed} slot
that drives the Gaussian-process proposals, and tune draws it from the
stream when it is not given -- so left alone, a fold's proposals would depend
on how much of the stream tune had consumed before reaching it. Here the
control is built inside the fold's seed scope with \code{seed} set to the fold's
tuning seed, the same number \code{.tuning_seed} reports. Fold \code{i} is exactly:

\if{html}{\out{<div class="sourceCode">}}\preformatted{set.seed(res$.tuning_seed[[i]], kind = "Mersenne-Twister",
         normal.kind = "Inversion", sample.kind = "Rejection")
tuned <- tune_bayes(object, resamples$inner_resamples[[i]],
                    iter = iter, initial = initial, objective = objective,
                    param_info = param_info, metrics = metrics,
                    eval_time = eval_time,
                    control = control_bayes(allow_par = FALSE,
                                            event_level = event_level,
                                            seed = res$.tuning_seed[[i]]))
final <- finalize_workflow(object, select_best(tuned, metric = <first metric>))
set.seed(res$.outer_fit_seed[[i]], kind = "Mersenne-Twister",
         normal.kind = "Inversion", sample.kind = "Rejection")
last_fit(final, resamples$splits[[i]], metrics = metrics,
         eval_time = eval_time,
         control = control_last_fit(event_level = event_level))
}\if{html}{\out{</div>}}

The caller's RNG state and generator kind are restored on exit, including
when the call errors. The same seed gives the same result serially and in
parallel, at any number of daemons.
}

\section{Differences from calling tune directly}{


There is no \code{control} argument. What \code{\link[tune:control_bayes]{tune::control_bayes()}} settles is
settled here by the arguments above, or forced.

Settable: \code{iter}, \code{initial} and \code{objective}, which are \code{\link[tune:tune_bayes]{tune::tune_bayes()}}'s
own arguments and reach it unchanged; \code{event_level}, which reaches the inner
\code{control_bayes()} and the outer \code{control_last_fit()} alike; and \code{eval_time},
which reaches the inner \code{tune_bayes()} and the outer \code{last_fit()}.

\code{initial} is a count only. tune also accepts an earlier \code{tune_grid()} result
there, and this function refuses one, for the reason the argument's
description gives.

Forced: \code{allow_par = FALSE} on both tune calls a fold makes, because
parallelism belongs over the outer folds; and \code{seed}, set to the fold's
tuning seed and built inside that seed's scope, as the section above
describes.

Not offered: \code{no_improve}, \code{uncertain} and \code{time_limit}, the three settings
that stop a search before \code{iter} is reached -- every fold runs the full
\code{iter}, or until its candidates are exhausted; \code{save_gp_scoring}, \code{verbose}
and \code{verbose_iter}, which would print from a mirai daemon where nothing
shows it; and the slots that would have nothing to act on here, for the
reasons \code{\link[=nested_tune_grid]{nested_tune_grid()}} gives.
}

\examples{
\donttest{
if (rlang::is_installed(c("recipes", "yardstick"))) {
  data(mtcars)

  # Two tunable steps, so the search has candidates to propose.
  rec <- recipes::step_pca(
    recipes::step_ns(
      recipes::recipe(mpg ~ ., data = mtcars),
      disp,
      deg_free = tune::tune()
    ),
    recipes::all_predictors(),
    num_comp = tune::tune()
  )
  wf <- workflows::workflow(rec, parsnip::linear_reg())

  set.seed(1)
  folds <- nested_resamples(
    mtcars,
    outside = rsample::vfold_cv(v = 3),
    inside = rsample::vfold_cv(v = 3)
  )

  set.seed(2)
  res <- nested_tune_bayes(wf, folds, iter = 2, initial = 3)
  collect_metrics(res)

  # What each fold searched: the initial candidates at `.iter` 0, then one
  # proposal per iteration.
  res$.grid[[1]]
}
}

}
\seealso{
\code{\link[=nested_tune_grid]{nested_tune_grid()}}, \code{\link[=nested_resamples]{nested_resamples()}}, \code{\link[tune:tune_bayes]{tune::tune_bayes()}}
}
