# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-31 (all checks green; advisories only — the same 18 references pages record no verification claim. Line caps hold: ROADMAP.md 59 against 60, LESSONS.md 49 against 50, PROFILE.md 119, the M38 archive summary 11. Both byte budgets are over and both got worse: ROADMAP.md 41,910 B against 24,000 and LESSONS.md 29,755 B against 20,000, the latter despite trimming M36's lesson to its uncovered remainder and folding M38's into an existing line rather than adding one. The compressible rows are spent, so closing the gap means graduating or dropping candidates — a user call, now four passes overdue. Since the last check: M38 shipped the recorded fold-label columns and is archived after one review round, D-036 was recorded, M36's lesson was trimmed because the 108-cell sweep now reds on the mistake its prescription warned about, and the `check_nested()` candidate row absorbed two M38 review findings)_

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M39 | `print()` shows the object, `summary()` says what it means | review | — | normal | milestones/M39-print-tibble-summary-split.md |
| M40 | A `nested_final_fit` answers `summary()` the way its sibling does | planned | M39 | normal | milestones/M40-final-fit-summary.md |
| M38 | A results object's own fold-label columns, recorded rather than guessed | done | — | normal | milestones/archive/M38-id-columns-recorded.md |
| M37 | The vctrs half, so `rbind()` stops claiming a design it never ran | done | — | normal | milestones/archive/M37-vctrs-invariants.md |
| M36 | Removing an outer fold's row stops producing a `nested_results` | done | — | normal | milestones/archive/M36-dplyr-invariants.md |
| M35 | The factor level a caller can name as the event | done | — | normal | milestones/archive/M35-event-level.md |
| M34 | The arguments a caller can hand through to `tune` | done | — | normal | milestones/archive/M34-forward-tune-arguments.md |
<!-- rows grouped by status, not sorted by ID; keep only the 5 most recent terminal (done/dropped) rows — older ones live in milestones/archive/ + git -->

## Candidates
<!-- unnumbered ideas; one line each: idea — added YYYY-MM-DD — links -->
- The summary printout points a user at `x$.notes`, which the printed summary does not carry — added 2026-08-31 — M39 review O1. `print_failures()` (`R/nested-results-print.R`) closes with ``See `x$.notes` for what went wrong.``; before M39's split the printed object was the results object and the advice ran, and the object bound to `x` in `print.summary.nested_results()` is the summary bundle, whose components are `outer_label`, `requested`, `completed`, `failures`, `selection`, `grids` and `estimate`. `warn_partial_summary()` (`R/nested-results.R`) emits the same sentence from a place where the advice still runs, so a repair scoped to the print method leaves the two texts divergent, and `test-nested-results-print.R`'s `expect_match(txt, "See .*\\$\\.notes")` passes on some repairs that are still wrong. Left out of M39 at its implement gate rather than fixed, because the repair falsifies the acceptance criterion pinning that output to what it emitted before and the milestone had already returned once. Promote alongside M40, which mirrors this printing for `nested_final_fit`
- Seven M37 review findings below the fix bar, grouped because two passes over `R/nested-results.R` and `tests/testthat/test-vctrs-compat.R` reach all of them — added 2026-08-31 — M37 review R4–R9, R11. **The reachable prototype:** `vctrs::vec_cbind_frame_ptype(x)` is exported and hands back a 3-row, 0-column object wearing the class and the run's description, whose `print()` reports "3 requested, 0 completed" before erroring on the missing `.completed` (measured 2026-08-31); RR04 Q4 judged the exposure negligible because vctrs marks that generic experimental and keyword-internal and no verb reaches it, and M37 corrected the comment rather than the shape. **The name-repair hole:** `vec_cbind(x, tibble::tibble(splits = 1:3), .name_repair = "minimal")` keeps the class with two columns named `splits`; `dplyr::bind_cols()` answers identically, so door parity holds. **The cast that stamps from the wrong side:** `vec_cast.nested_results.nested_results()` reads the record off `x` where every other rule in the file reads it off the template; no reachable harm, `vec_restore()` re-deciding after it. **Three test-side gaps:** eight blocks call `tibble::` with no `skip_if_not_installed("tibble")` though D-034 put it in Suggests; `expect_no_record()` loops `results_attributes()` only, so neither private carrier is asserted absent on a shedding path (swept 2026-08-31, no leak today); and `rbind.nested_results()` templates on `args[[1]]` positionally and promotes every data-frame argument to a tibble, so the result's class depends on argument order. **One cost item:** `names<-.nested_results()` runs the full sort-and-compare rule on every internal name assignment. D-032's Consequences paragraph is separately stale on the refusal claim, which D-035 annotates in place. Promote on the next pass over the compatibility methods, or on a user reaching any of the first three
- Ask the daemon pre-flight what the WORKFLOW needs, not only whether it holds nestedtune — added 2026-08-31 — the #37 hotfix (PR #44). `daemons_load_status()` probes one package, this one, and #37 showed the gap: a daemon can pass the pre-flight cleanly and still be unable to run a single fold. The hotfix closes the *attach* half by calling `library()` on `tune::required_pkgs(object)` in every daemon (`attach_daemon_pkgs()`, `R/parallel.R`), and warns naming any package that would not attach — but that warning fires after the pre-flight has already passed, and a daemon whose library genuinely lacks the package still reaches the fold and fails there. Folding the workflow's packages into the pre-flight's single round trip would make it one refusal before dispatch, with the remedy the incompatible-build branch already words well. Promote when a user hits the missing-rather-than-unattached case, or alongside the remote-pool row below, which asks the same probe a second new question.
- Generalize the orchestrator past `tune_grid()` — added 2026-08-31 — [#35](https://github.com/tidymodels/nestedtune/issues/35) (topepo), D-002, D-010. The inner loop is hard-wired to `tune::tune_grid()`; topepo wants `tune_bayes()` and the rest reachable without a `nested_tune_bayes()` echoing the whole orchestrator, and points at finetune's `nested` branch prototype, which carries the tuning function as an argument and assembles the call with `rlang::call2()`. D-010 fixed `nested_tune_*` as the family's naming convention and rejected `tune_nested()`; D-002 fixed the delegation boundary this would widen. Depends on the `control`/`...` question the row above owns, since a Bayesian inner loop needs `control_bayes()` slots that `control_grid()` has no counterpart for. The largest of the four asks and the one most likely to want an RB.
- A method reporting how often each candidate was selected across the outer folds — added 2026-08-31 — [#36](https://github.com/tidymodels/nestedtune/issues/36) (topepo), M08, M22. The facts are on the object already: `.selected` holds each outer fold's chosen parameters and `autoplot(type = "parameters")` draws exactly this spread (M08, "Selection instability you can see"); what is missing is a tabular API returning it. topepo floats `consensus()`, `agreement()` or the existing `tidy()` generic as names. M22's gate declined a `nested_results` method for the two `extract_` generics on the ground that pooling per-fold records asserts a menu M21 measured false — that objection is about the candidates scored, not about the selections, so it does not carry over, but the naming question it settled (this package OWNS its generics, D-023) does.
- Refuse a nested design carrying columns beside `splits`, `inner_resamples` and its `^id`-named fold labels — added 2026-08-31 — M38 Out, from M38's criteria audit. `check_nested()` (`R/checks.R:129`) admits any design carrying some `^id`-prefixed column, and M38 makes every column the constructor copies beside `splits` and `inner_resamples` a fold-label column — record, printed label and order key alike. For a design from `nested_resamples()` or `rsample::nested_cv()` those columns are exactly `id`, or `id` and `id2`, so nothing changes; a design built elsewhere carrying, say, a `weights` column would have it pasted into every fold's label and keyed on. Refusing that design at the entry gate is the narrow fix, and it costs a new user-facing refusal with its tests. **Two M38 review findings sit on the same pass** (O1, O8): a classed object whose record is empty answers with a malformed table rather than refusing — `fold_ids()` gives `character(0)` and `collect_metrics(summarize = FALSE)` then returns a tibble reporting 0 rows with column lengths 0, 6, 6, 6 (measured 2026-08-31), while `print()` on such an object with a failed fold indexes past the end in `print_failures()` (`R/nested-results.R:824`, `:798`; `R/nested-results-print.R:96`) — **O8 is taken by M39**, which relocates `print_failures()` behind `summary()` and fixes all three forms of an unusable label record, so this row keeps O1 and the `new_nested_results()` gap only — and `new_nested_results()` itself asserts nothing about a design carrying no label columns, which `check_nested()` blocks only on the public path. Deserializing a results object built before M38 is the reachable path to the first. Promote when a design built outside rsample reaches the orchestrator, or alongside the row above, which asks the same question of the pre-flight.
- The pkgdown workflow external PR #30 collapsed, and the three M17 findings that came back with it — added 2026-08-31 — M35 review P1–P3. `.github/workflows/pkgdown.yaml` is now one `pkgdown` job that both runs `build_site_github_pages()` (executing the ref's vignette and `@examples` code) and carries job-level `permissions: contents: write`; M17's review split exactly that apart (F2) and D-022 rejected the `any::pkgdown` line the same rewrite restored, which makes `DESCRIPTION`'s `Config/Needs/website` decorative again (F1). `topepo` left the question open on the PR hunk itself — "I wasn't sure if any of this should be retained" — and it is still unanswered. None of this is M35's diff; it reached the default branch on 2026-08-31. Three existing rows below now describe a structure that no longer exists (the deploy job to exercise, the Node 20 SHA pin's location, the CI records' job names), so whatever answers this row should re-point them.
- The fixture-cache key's signature enumeration no longer enumerates — added 2026-08-31 — M35 review O3. `tests/testthat/test-fixture-cache.R:230-238` is titled "the key separates every fixture signature this suite asks for" and its comment claims every distinct combination the converted files request, but M35's four `cls_*` signatures are absent and no pair in the list differs only by `event_level` — the separation `test-event-level.R`'s four memoised runs depend on. `fixture_key()` hashes all matched arguments, so the behaviour is right today; nothing pins it. Raised at M34's review as finding 5 and again at M35's.
- Four M34 review findings below the fix bar, plus the suite's unexplained wall-clock — added 2026-08-31 — M34 review F5, F6, F7, F10. Grouped because one pass over `tests/testthat/test-dots-barrier.R` reaches the first two: AC5's registry-derived method probe guards its domain only with `expect_gt(length(probed), 0L)`, so eight of the nine methods could stop being registered and it would still pass where AC1's recorded formals vector would not; and AC6's positional filter (`!any(grepl("=", args[-1L]))`) classifies a call with a positional second argument and any later named argument as non-positional, and trips on `>=`/`<=`/`==` inside any argument, while its text scan counts a `collect_metrics(` in a comment or string as a call site. Separately, `R/nested-results.R:31-32` records `grid` and `metrics` as attributes but nothing records `param_info`, so two runs differing only in the restriction are indistinguishable from what the object carries (the per-fold `.grid` column covers most of it); and `extract_tune_results(obj, foo = 1)` now reports the dots error rather than `abort_no_extract_method()`'s "no method for this class", which is the diagnosis a user holding the wrong object needs — a consequence of the fence AC5 asked for, not a defect in it. **The wall-clock, unexplained:** the full suite measured 561s standalone and `[341s/599s]` under `R CMD check` on 2026-08-31, against `64s/100s` recorded when M34 was implemented and `[71s/110s]` on the first of the same day's two review checks; every test green and none skipped in the slow runs, and the five documentation and test-hygiene edits between the two checks cannot account for it. It stays inside PROFILE's 20-minute `check-r-package` cap. Promote on the next CI-economy audit, on a check leg approaching the cap, or when something needs `param_info` on the results object
- Forward `eval_time`, and accept a `control` argument, on the two orchestrators — added 2026-08-30 — M34 Out; the `event_level` half was taken by M35 on 2026-08-31, and [#33](https://github.com/tidymodels/nestedtune/issues/33) (topepo, 2026-08-29) asks for the rest of it: the `...` barrier and `param_info` shipped with M34, so the `control` slot he names is all that is left and D-030 is the standing answer to it. This row owns the issue. `tune::tune_grid()`, `tune::select_best()` and `tune::last_fit()` all take `eval_time`, which dynamic survival metrics need; verifying it behaviourally needs a censored-regression fixture, so `censored` and `survival` join Suggests through their own gate and D-entry, and the mock-capture alternative binds the harness rather than the deliverable and cannot reach a mirai daemon. **The `control` premise, corrected 2026-08-31 at M35's plan gate:** accepting tune's object buys less than this row claimed. Of `control_grid()`'s ten slots, `save_pred`, `extract` and `save_workflow` land on the inner `tune_results` the fold record discards on success (`R/nested-tune-grid.R:435-444`); `allow_par`, `parallel_over`, `backend_options`, `workflow_size` and `pkgs` are forced or inert under `allow_par = FALSE`; a daemon's `verbose` output is not shown; and `control_last_fit()` (tune 2.1.0) carries no `save_pred` at all, only `verbose`, `event_level`, `allow_par`. `event_level` was the one slot that changed a reported number, and M35 exposes it as its own argument rather than through a settings object eight of whose slots are inert here. Promote on a user needing a dynamic survival metric, or on the inner tuning run being retained, which is what would make `save_pred` and `extract` observable.
- The tidymodels conventions M32 and M33 deliberately leave behind — added 2026-08-28 as the whole convention row, address half shipped 2026-08-30 (M30), community files, site template and lifecycle badge taken by M32 and the shared CI workflows by M33 on 2026-08-30 — D-025, D-026, M30. Four items, each with its own reason for waiting. **Posit attribution:** every sibling `DESCRIPTION` carries `person("Posit Software, PBC", role = c("cph", "fnd"), comment = c(ROR = "03wc8by49"))` and their LICENSE files name Posit as copyright holder, where this repo names Jeffrey Girard and D-025 retains him as owner and primary maintainer; the siblings' `supported-by-posit` `in_header` badge script rides on the same question. **Site analytics:** all nine siblings also carry a Plausible `in_header` block, which M32's review flagged as ruled on by nothing — omitting it stays the conservative default; promote on an organization answer about whether member sites are expected to report to it. Promote on an answer from the tidymodels maintainers — the user's plan-gate answer was to ask first, not to adopt or refuse. **`.github/CODEOWNERS`:** rsample and tune carry one naming organization understudies (tidyverse.org/development/understudies); promote once understudies are assigned here. **`README.Rmd`:** all nine surveyed siblings knit `README.md` from one and this repo hand-writes it, so its examples go unexecuted; promote alongside the p > n vignette re-cut row, which touches the same prose. CRAN and downloads badges belong to the first release, not here. **`R-CMD-check-hard.yaml`:** carried by 3 of 9 siblings, it is the one org workflow with `push` and `pull_request` triggers, so it needs the shared `paths-ignore` block or `.github/ci-usage.py` refuses to run — and Suggests holds mirai, ranger, recipes and yardstick, most of what the suite needs, so what a Suggests-free check exercises here is unestablished. Promote on evidence a Suggests-free load path is reachable
- Route fold data to daemons through `mori`'s shared memory instead of by value, keeping the by-value path as the remote fallback and the one-frame-per-fold invariant gate regardless — added 2026-07-31 — M26 Out. `references/mori-backend-assessment.md` (P1-P9) owns the derivations over `benchmarks/mori-wire-manifest.json`; measured at M26 against mori 0.2.2 / mirai 2.7.2 per fold on M23's fixture, captured from the real dispatcher on the package installed with srcrefs stripped _(figures corrected 2026-08-01 to the manifest's installed-state capture; the dev-state originals, a `load_all()` artifact, are in git)_: current path ~941.7 kB / 1 copy against a modelled mori bundle at ~103.1 kB / **0 copies**, a factor of **9.13**, with the gap ~838.6 kB dominated by the data itself; installed, the worker closure is 524 B and near-negligible on both routes. Fold records stay `identical()` to serial at 2 and 3 workers, so IP2 and D-011 are untouched and D-018 is not revisited. Four things the row carries forward: adoption deletes only the blanking and rehydration (`R/parallel.R:144-183`, 40 lines) and must say what replaces `is_fold_payload()`'s fat-path fallback, which is the invariant gate M23 review F1 showed tunes on the wrong rows without; mori is same-machine, so the by-value path stays for remote pools; mori's `Depends: R (>= 4.3)` against this package's `R (>= 4.1)` either raises the floor or goes in conditionally; and it is a dependency change needing its own gate and D-entry. Supersedes the `mirai::everywhere()` preload row below if taken. Promote on evidence that per-fold transfer is a real cost on a user's run, or on tune adopting mori upstream (tune#1188)
- Let the metric that selects differ from the metric that scores, instead of one `metrics` argument driving both loops — added 2026-07-31 — `references/stone1974a.md`, §2 item VII (p. 116), which owns the quotation and the Table 1 (p. 119) figures. `nested_tune_grid()` takes a single `metrics` and passes it to `tune_grid()` at `R/nested-tune-grid.R:363` and to `last_fit()` at `:385`, so the inner loop always selects under the metric the outer loop reports. Stone states the two need not match and measures cases where matching is worse. Not a defect: coupling is the one obvious path GP3 prefers, matches tune's idiom, and decoupling needs a second argument plus a story about which metric `collect_metrics()` reports and which `.selected` was chosen under (M20/M21 both assume one metric set). Evidence is 1974, n = 7, location estimation — nothing establishes it pays for a tidymodels workflow. Promote on evidence a real workflow selects better under a different metric than it reports, or on a user asking for it
- Detect daemons running different code that still carries the same symbols — a build hash or fingerprint rather than symbol presence — added 2026-07-30 — M24 Out. M24's capability probe asks whether the symbols `dispatch_folds()` resolves by name are present, which catches the observed failure (a stale daemon with no `rehydrate_payload`) but not a daemon whose symbols all exist and whose behaviour differs; that case breaks IP2 invisibly, since the same seed would give a different answer depending on which daemon took the fold. A version comparison cannot substitute: `DESCRIPTION`'s `Version:` has read `0.0.0.9000` since M01 (`fafb31f`, the only commit touching it in 23 milestones), so a stale daemon reports the host's own string. Promote on any evidence a heterogeneous pool has produced differing results, or once the package versions releases
- Stop paying for source references on the wire — the worker closure `dispatch_folds()` sends is 203,790 B under `pkgload::load_all()` against 524 B re-parsed, because a function's srcrefs serialize with it and their presence depends on how the package was installed rather than on the run — added 2026-07-30 — M23 Out. `utils::removeSource()` at dispatch was implemented and measured at M23, then removed: `utils` is not in Imports, a dependency change needs its own gate (tracking-rules), and the gain is development-only since `install.packages()` defaults to `keep.source.pkgs = FALSE`. The same closure was equally srcref-laden as `.f` before M23, so this is not a regression M23 introduced. Promote if a dependency gate opens `utils` for another reason, or on evidence a user installs with srcrefs kept
- Generate the candidates rather than read back the ones that scored, so a candidate that failed on every inner resample is recorded too and outer folds stop searching different grids — added 2026-07-30 — M21 Out, absorbing the M20 row it supersedes; M21's archive summary owns the measurements. Two facts on tune 2.1.0: a `tune_results` carries no record of its expansion, so a candidate that failed everywhere leaves no trace in `.metrics`; and expansion is stochastic for a continuous parameter, so with the default `grid = 10` every outer fold searches a different menu under its own seed. Two shapes weighed and both declined: expanding per fold inside the worker duplicates tune's own expansion (GP1) and shifts results under an unchanged seed; expanding one shared grid up front finalizes parameter ranges from data including each fold's assessment rows (RB tripwire: ip-touching). M21 records the difference as an observation rather than asserting it, IP2 declining to guarantee anything across tune versions. Promote on any evidence a user needs the attempted-but-failed candidates, or reads the per-fold records expecting one shared grid
- A resampling specification can no longer see its own calling frame: `nested_resamples()` evaluates `outside`/`inside` in a child environment binding `.nestedtune_data`, so `parent.frame()` inside the specification is that transient environment rather than the caller — added 2026-07-30 — M18 review finding A2, scored 83 and accepted rather than fixed, because binding the data to a name is what removes the frame from the error message and the two cannot both hold. Reproduced at review: a spec function testing `identical(parent.frame(), globalenv())` returned TRUE before M18 and FALSE after. Narrow — a tidymodels resampling constructor does not inspect its caller — and it also means a spec capturing `match.call()` for later stores a call naming `.nestedtune_data`, unresolvable once the environment is gone. Promote on any evidence a real specification depends on its calling frame
- Refuse a design for more than its two columns' element classes — a zero-row inner `rset`, an `inner_resamples` length mismatched to `nrow(resamples)`, an `id` column disagreeing with the splits — and name every offending position rather than the first — added 2026-07-30 — M19 Out. M19 takes only the two class checks, which are the shapes with a measured baseline; these are unmeasured, and the every-position half is a message rewrite rather than a new check. Promote on any evidence a design reaches a driver malformed in a shape the class checks pass
- Two metrics loose ends in the results object, grouped because one pass reaches both — added 2026-07-30, trimmed to this remainder 2026-07-30 at M20's plan gate. `nested_results` retains no inner tuning run, so nothing on its public surface shows what selection saw (the `nested_final_fit` half of that is M22, promoted 2026-07-30 from the `extract_`-accessor row this line used to point at); and the shared `reg_metrics()` still equals tune's regression default, whose original rationale is weakened now that M18's `sep_*` fixture gives the suite a metric set that separates — changing it would re-record snapshots across ~6 files and still miss the `tune_grid()` site. M20 took the third item, the unread `attr(out, "metrics")`, and found `attr(out, "grid")` in the same position. Promote when a second milestone needs the same metric separation from a different fixture, or when something needs the retained tuning run
- Name the cause of the two pre-M14 stalls — `R CMD check` on macOS 52 min and `covr` on ubuntu 40 min, same signature each: everything before the suite passing, `* checking tests ...`, then silence, with a re-run of the identical tree passing in normal time — added 2026-07-27 (`0db6f5f`), trimmed to this remainder 2026-07-27 at M16's plan gate. M12 took the mitigation (`timeout-minutes: 20`), M14 the localization, M15 the one confirmed defect, M16 the two later occurrences; see their archive summaries. What is left is the two occurrences predating M14's `collect_bounded()` sweep, when a bare `[` collect could block forever and no bound covered it — so they may be a genuinely unbounded phenomenon M16's budget does not reach. Two suspects were refuted by execution at M16's plan gate and the refutations are in the M15/M16 lessons line. Not `/hotfix`-able: no test can be written that fails first. Promote on a recurrence whose duration exceeds the worst case M16's ledger establishes
- Share one mirai worker pool across `test-parallel-identity.R`'s tests instead of restarting it 8 times, each restart reloading the package into every daemon — added 2026-07-27 — M12 Out, declined at its plan gate. The file is 40.5s, 17.5% of the suite, and the pool restarts are most of it; but the tests it holds are what prove IP2 (same seed, same answer, any worker count), and leaked state across a shared pool would corrupt exactly that guarantee. Promote on evidence that a reused pool is observably clean between tests — a probe showing no state carried across a reuse, which M14's suite-end pool assertion (AC4) may supply
- Take the pre-flight deadline off the wall clock: `R/parallel.R:477-478` computes it from `Sys.time()`, so a backwards system-clock step silently extends the only bound in the package — added 2026-07-27, corrected 2026-07-30 at M24's plan gate. This row previously said "`proc.time()` is the monotonic substitute"; that was asserted, never verified, and M24's criteria audit found R documents `proc.time()`'s elapsed field only as real time since process start, computing it as `currentTime() - R_StartTime` where `currentTime()` reads the same realtime source `Sys.time()` does — so it is not established as step-immune and a `monotonic_now()` built on it may not be monotonic. M24 declined the item on that basis rather than ship a misnamed helper. Promote on evidence the bound has overrun in the field, or once a genuinely step-immune R-level clock is established
- Cut the CI matrix on pull requests to ubuntu+windows, and cancel superseded runs on the default branch too — the two halves of the M11 candidate row's agreed shape that M11 dropped at its gate — added 2026-07-27 — M11 Out. The row's premise was wrong: this repo is public, so standard-runner minutes are free and the ×10 macOS multiplier does not apply (`billable: 0` on every run). Measured against dropping it: the PR cut saves a median of 0.0 min wall-clock because windows is the critical path in 43 of 55 full runs, while losing pre-merge R-devel and oldrel-1 coverage; cancelling on `main` reclaims 17 runs / 372 min but can leave a default-branch commit carrying no completed check. Promote on evidence that either premise has failed — the repo goes private, or windows stops being the wall-clock critical path
- Keep the R-devel dependency cache alive across quiet weeks — GitHub deletes any Actions cache not accessed for 7 days, and a devel leg starting cold pays a source build of 129 packages where a warm one takes 9m28s (measured 2026-08-01) — added 2026-07-27 as an Actions-cache-pressure row, premise corrected and the row re-pointed 2026-08-30 at M31's plan gate — M11 Out, M31 Out. Its original premise, recorded as unmeasured, was eviction from GitHub's 10 GB per-repo limit at "5.82 GB across 61 active entries"; `gh cache list` measured 1.70 GB across 17 entries on 2026-08-30 with no R-devel (4.7.0) entry at all, so what evicted it is the idle expiry. M31 fixes the cap so a cold build completes and writes the cache, and accepts paying it again after a gap; the shape left to price is a scheduled warm-up run against a fifth workflow's recurring minutes and its own red surface. Promote on evidence that cold devel builds recur often enough to dominate the leg's wall-clock, or if the cache total nears 10 GB
- Four published-site rows, grouped because one pass over `.github/workflows/pkgdown.yaml` and the `docs/` publication it drives reaches all of them — added 2026-07-27, grouped 2026-09-01 at M39's cap gate — M17 Out, M17 review F4, F5, F6, F10, D-003, D-028. **The Node-20 pins:** the SHA pin on JamesIves/github-pages-deploy-action v4.5.0 targets Node 20, which runners now force onto Node 24 with a deprecation annotation on every deploy; the pin is deliberate (M17 review F10) because this is the one action in the repo holding a token that can write to it and tags are mutable, so the remedy is moving the pin to a newer release's SHA rather than dropping it. Promote when the annotation becomes a failure, or on the first release of the action whose SHA is verified to target a supported Node. Absorbed 2026-08-30 (M33): three more token-holding actions ride moving tags — `r-lib/actions/pr-push@v2` under `contents: write` in `pr-commands.yaml`, and `posit-dev/setup-air@v1` and `reviewdog/action-suggester@v1` under `pull-requests: write` on `format-suggest.yaml`'s `pull_request_target` trigger — vendored that way because M33's first criterion holds each file at the organization's shared blob and a sha pin puts it off that blob (D-028). Promote on the organization pinning them upstream, or on evidence that a moving tag under a write token has been altered. **The unexercised deploy job:** `if: github.ref_name == default_branch` means no pull-request run can execute it, so its first execution ever was a publish that failed on a green PR; the hotfix's regression test asserts the one requirement now known (a checkout precedes the deploy action) and nothing exercises the rest, and a `workflow_dispatch` publish to a scratch branch is the shape to price against just extending the static assertions. Promote on the next deploy-job failure whose cause the static test could not have named. **The two-name hygiene list:** the absent-assertion checks only `docs/CLAUDE.html` and `docs/ci-usage-baseline.html`, not `search.json`/`sitemap.xml`, so a revert to the rejected post-build-delete shape would publish `CLAUDE.md`'s full text to the site's own search box with a green run; the `rm` and the assertion both hardcode two filenames, so a later `AGENTS.md` at the root publishes silently; and `clean: false` makes publication additive, so anything that reaches `gh-pages` once stays served forever, including a page renamed away pre-1.0 under D-003. Promote on any repo-internal markdown file being added at the root or in `.github/`, which is the event the hardcoded list cannot survive. **The `/dev/` site:** at `0.0.0.9000` pkgdown's default `mode: release` builds to the site root, which is what M17 ships and what the advertised URL points at, while `mode: devel` moves it under `/dev/` and leaves the root empty until a release exists, which is why M17 keeps the default. Promote on the first tagged release, whose site would otherwise overwrite the development pages at the same URLs
- Bring the CI records up to three workflows — added 2026-07-27 — M17 review findings F9 (65) and F13 (35), both below the action threshold. `.github/ci-usage-baseline.md:5` still records the path filter as "read from R-CMD-check.yaml, test-coverage.yaml" when three workflows now declare it, and M11's baseline numbers (108 runs / 2276 machine-min / 324 jobs) were measured before a fourth workflow existed that runs ~2m30s on every qualifying push and PR; `cairn/PROFILE.md`'s test-doctrine slot still describes CI as the usethis pair plus four divergences, so a reader of that slot alone does not learn the third workflow exists. Absorbed 2026-08-30 from M31 review finding F5: the slot now carries three `timeout-minutes` numbers copied from `.github/workflows/`, guarded only by a prose instruction to re-read them, where the M16 drift lesson asks for a check that re-reads the call site — `.github/ci-usage.py` already runs that shape for the six `paths-ignore` copies and is where a cap cross-check would go. Promote on the next CI-economy audit, whose comparison against the stale baseline would read the added workflow as a regression in the filter. **Absorbed 2026-08-31 (M37, RR04 recommendations 3 and 5):** a fifth workflow is now wanted for a different reason — `vec_cbind_frame_ptype.nested_results()` is the only lever that makes a column added through vctrs keep the class, and vctrs marks that generic experimental and internal (D-033), so a check leg against development vctrs would surface a contract change within a day of upstream moving rather than at the next local run; its recurring minutes and own red surface are the cost this row's audit prices. The same recommendation pair carries an upstream ask that would make the leg unnecessary — on r-lib/vctrs, either stabilize the generic (vctrs' own `sf` method has depended on its unchanged contract since 2020) or have `vec_cbind()` restore against its first input's full type, which `dplyr::bind_cols()` already patches in by hand. Promote either half on any vctrs release note touching `vec_cbind()`, or on the vctrs compatibility test going red
- Stop sending the recipe's own retained copy of the training data to every fold — `recipes::recipe(formula, data)` keeps all training rows in `$template`, and the workflow rides in `mirai_map()`'s `.args`, which is serialized once per task — added 2026-07-30 — M23 Out. Measured at M23's plan gate: a recipe workflow over a 5000x21 frame serializes to 847,146 B against 840,540 B of data, one sentinel copy, so once M23 leans the payload the template is the largest remaining term on the wire for a recipe user (5 folds: 4.24 MB of 8.93 MB, which is why M23's 25% bar is set against a formula fixture and would read 29.8% here). Left out because trimming it is surgery on a recipes-owned object whose other readers of those rows are unestablished, which GP1 argues against. Promote on evidence that no recipes code path reads `template` for its rows rather than its structure, or on a user report of dispatch cost dominated by a recipe
- Preload the data into each daemon once per run rather than once per fold, with `mirai::everywhere()` — added 2026-07-30 — M23 Out. Measured at M23's plan gate by reading `mirai::mirai_map()` (mirai 2.7.2): `.args` is serialized once per task, not once per run, so M23's stateless shape has a floor of one data copy per fold and a preload would send one per daemon instead — 2 copies rather than 5 on a 5-fold run at 2 daemons. Declined because it makes the user's own pool hold state across folds and across runs, needing cleanup and carrying a stale-object hazard M23's shape has none of. Promote on evidence that per-fold `.args` transfer, rather than the retained recipe template above, is the dominant remaining cost on a realistic run. **M26 assesses `mori`, which answers the same question a third way (shared memory rather than preload or per-task transfer) and would supersede this shape if adopted**
- Probe remote mirai daemon pools, which nothing verifies today — RR03 Q5 established the load requirement by execution only for local daemons and marked the remote case *inferred* from the same mechanism, so a remote host missing the package still surfaces as opaque per-fold worker failures — added 2026-07-26 — M10 Out, carried over from the absorbed pre-flight rows; needs a remote host to test against before it is plannable. **M26 records the consequence of `mori` being same-machine-only, which bears directly on whether a remote pool stays a supported shape**
- `predict()` and `augment()` methods on `nested_final_fit`, so the object answers directly instead of only through `extract_workflow()` — added 2026-07-26 — M05 Out; D-014 left them off deliberately, matching what `tune::last_fit()` asks of users
- Make a design's stored `inside` call survive leaving its construction scope, by substituting its arguments' values at construction rather than capturing the frame — added 2026-07-26 — RR02 B1; M05 hit this on the repo's own `det_nested()` helper, so any wrapper parameterizing `v` is affected. M05 refuses loudly and asks for literals (AC11); capturing the frame instead would retain it, which GP4 argues against. Does not reach `rsample::nested_cv()` designs
- Variance estimation / inference on the nested estimate — added 2026-07-25 — G6. The caveat half (M02 review finding F5, the naive `std_err` shipping with no roxygen caveat) left for M25 on 2026-07-31; only the interval half remains here. The literature is on the shelf and each page owns its own numbers — `references/` bates2023, krstajic2014, bengio2004, bayle2020, austern2020, cawley2010, luo2025, luo2026, bayle2026, nachum2026, tsamardinos2018, gauran2025. What this row carries is the synthesis no single page owns: **four blockers, all of which a plan must clear.** (1) Loss stability for a tune-then-fit procedure is unestablished, and the shelf splits the notion five ways (see the stability-synthesis row below); luo2025 shows the closest one is uncertifiable from data without exhaustive search, and bayle2026 shows the condition is a ratio that fails for comparisons of two individually stable algorithms — so nothing here may ever test "workflow A beats workflow B". (2) The valid interval's estimand is the k-fold test error of the rules this run fitted, not the error of the final model. (3) The estimators need per-observation losses, and `R/nested-tune-grid.R:385-396` keeps only `collect_metrics(fitted)` per outer fold, discarding `last_fit()`'s `.predictions` — retention cost must be priced against M23's lean payload, and tsamardinos2018's BBC-CV needs the same rows, so two unrelated methods hinge on them. (4) Categorical, not practical: luo2026 bounds the power of any assumption-free valid black-box risk test, and v-fold nesting runs at the ratio where such an interval cannot exist — "nobody has built it" was the wrong reading. One favourable datapoint (bayle2026 Fig. 3, a CV-tuned Lasso measured relatively stable from n = 90 to 90,000) and one closed route (gauran2025's valid CI rests on a ridge hat-matrix closed form the contract boundary excludes). Promote only on all four blockers clearing
- Re-cut the nested-CV vignette onto a p > n example where nesting demonstrably earns its cost — added 2026-07-31 — M25 Out. M25 corrects `vignettes/nested-cv.Rmd:81-84`'s claim that `mtcars` is where the optimism is largest rather than changing the example, because `references/tibshirani2009.md` (bias material only at p >> n), `references/vabalas2019a.md` (feature-to-sample ratio predicts overfitting; mtcars sits at ~0.31, the low end of its 1/3-20 sweep) and `references/wilimitis2023.md` (a measured null at n >> p with a small grid) put the worked example in the regime where nesting buys least. A wide example would make the claim true instead of corrected, but M06 built every prose number as inline `r` over mtcars plus a 6-point ranger grid, so the re-cut invalidates both autoplot figures, the write-up template and the runtime budget
- An analytic oracle for the outer loop via `parsnip::null_model()` — added 2026-07-31 — from `references/nachum2026.md`. Theorem 4.10 plus Lemma 4.9 give an exact closed form for the k-fold CV MSE of the majority algorithm under Bernoulli(1/2) labels: MSE = ((k-1)/k)*Cov(n,m) + 1/(4n) with Cov(n,m) = 2^-n * sum_j C(m-1,j)^2 * C(n-2m, floor((n-m)/2) - j). `parsnip::null_model(mode = "classification")` instantiates that algorithm and carries no tuning parameters (verified present 2026-07-31). Would be the shelf's first GP2 type (3) oracle for the outer loop. Two unmeasured risks before it is plannable: `null_model()`'s tie-breaking at Y = n/2 is unchecked against the theorem's `Y > n/2` rule, and MSE is a distributional property, so the replicate count must be priced against GP4 the way `references/varma2006.md`'s null-data invariant already was
- A synthesis note reconciling the shelf's five stability notions — added 2026-07-31 — hypothesis and loss and squared-loss (`references/nachum2026.md` Definitions 2.1-2.3), prediction-based (eps, delta) (`references/luo2025.md`), and *relative* loss stability, a ratio rather than a bound (`references/bayle2026.md`). They are not interchangeable and results proved under one do not transfer to another, but the G6 row above still says "loss stability" as though it named one condition. `references/` owns synthesis notes; this is what stops a future G6 plan conflating them
- Document IP2's enforceable scope in DESIGN.md — it binds only randomness flowing through R's RNG, so engines that bypass it (kernlab SVM, keras/torch) are unreachable by any R-side scheme — added 2026-07-25 — RR01 B4; amending IP text needs a D-entry
<!-- drift-check: lean_bundle_bytes=941.7 kB; mori_bundle_bytes=103.1 kB; ratio_lean_over_mori=9.13; gap_bytes=838.6 kB; worker_closure_bytes=524 B@2 -->
