# M55: Every driver refuses a design whose inner resamples are empty, whose fold labels do not uniquely name its folds, or whose columns are not an rsample design's

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** GP3
- **Resolves:** —
- **Branch/PR:** `m055-design-entry-checks` · https://github.com/tidymodels/nestedtune/pull/65

## Goal

The five orchestrators refuse, at the call the user wrote and before any fold runs, a nested design whose inner `rset` has no rows, whose fold labels hold an `NA` or repeat across rows, or whose label columns are not what rsample's readers would find — naming every offending position.

## Scope

User-facing tier: the refusals fire from the five exported drivers, and the `resamples` contract is documented for their callers.

**In:** `check_nested()` (`R/checks.R:108`), called at the entry of `nested_tune_grid()`, `nested_tune_bayes()`, `nested_tune_race_anova()`, `nested_tune_race_win_loss()` and `nested_tune_sim_anneal()`, gains three refusals: an `inner_resamples` element that is an `rset` with zero rows; a label column (any column beside `splits` and `inner_resamples`) whose name does not match `(^id$)|(^id[1-9]$)` — the pattern rsample's and tune's `col_starts_with_id()` read id columns by (rsample 1.3.2, tune 2.1.0) — or that is neither character nor factor; and label values that are `NA` or a label tuple two rows share. Every refusal `check_nested()` raises, the two existing element-class refusals included, carries the condition class `nestedtune_bad_design`, the exported driver's call, and every offending position or column. The `resamples` documentation, shared by the five drivers through `@inheritParams nested_tune_grid`, states the contract; `NEWS.md` records the refusals. D-047 records the contract and annotates D-036's consequences clause.

**Out:** an inner `rset` built over a frame other than its outer split's, and inner splits that are not `rsplit`s → the trimmed candidate row (the parallel path already routes the first down the fat path, `R/parallel.R:105-118`). An `inner_resamples` length that disagrees with the row count → dropped as impossible, a data frame's columns sharing one row count. Refusing malformed results objects at the readers → DESIGN.md Known issues (M53's acceptance). A design built by `nested_resamples()` with a malformed `outside` → the constructor's own refusals (M18), unchanged here.

## Acceptance criteria

- [x] AC1: A design in which any `inner_resamples` element is an `rset` with zero rows is refused by each of the five drivers with a condition of class `nestedtune_bad_design`; one test per driver plants a zero-row `rset` (`rsample::manual_rset(list(), character(0))`) at the first and at the last position of `det_nested(d)` (three outer rows) in turn, asserts the class each time, and asserts the unaltered fixture is not refused.
- [x] AC2: A design whose label columns hold an `NA`, or in which two rows carry the same label tuple, is refused by each of the five drivers with a condition of class `nestedtune_bad_design`; one test per driver plants an `NA` at the first and at the last position of `det_nested(d)` in turn, and a copy of row 1's tuple at the last position, asserting the class each time; a control asserts `repeated_design()` (`tests/testthat/helper-orchestration.R:583`; `id` repeats across repeats while the `(id, id2)` tuple is unique) is not refused.
- [x] AC3: A design with a label column that is neither character nor factor, or with a label column whose name does not match `(^id$)|(^id[1-9]$)`, is refused by each of the five drivers with a condition of class `nestedtune_bad_design`; one test per driver plants, once before `id` and once after it in column order, an integer `id2`, a character `weights`, a numeric `weights` and a list `extra`, and replaces `id` in place with an integer column, asserting the class each time; a control asserts a `nested_resamples()` design and an `rsample::nested_cv()` design carrying `id` and `id2` are not refused.
- [x] AC4: Every refusal `check_nested()` raises — enumerated as every `cli_abort()` call reached from its body, listed by grep over `R/checks.R` — carries the class `nestedtune_bad_design`, carries as its `call` the exported driver the user called (the race pair naming `nested_tune_race_anova()` or `nested_tune_race_win_loss()`, never their shared internal), and names every offending position or column; tests plant offenders at all three positions of `det_nested(d)` for each row-wise refusal, the two element-class refusals included, and assert all three positions appear in the message; plant two offending columns for the column refusals and assert both names appear; and assert `conditionCall()` names each of the five drivers.
- [x] AC5: Each design the suite's fixture constructors build — `det_nested()`, `valid_folds()` (`test-nested-tune-grid-checks.R:4`), `repeated_design()`, and `nested_resamples()` and `rsample::nested_cv()` called directly — returns invisibly from `check_nested()`, asserted by a test naming each; and `devtools::test()` is clean.
- [x] AC6: `NEWS.md` states the five refusals (zero-row inner `rset`; `NA` label; repeated label tuple; label column not character or factor; label column named outside the id pattern), and the `resamples` parameter documentation of `nested_tune_grid()` states the contract those refusals enforce; `devtools::document()` produces no diff and `devtools::check()` reports 0 errors, 0 warnings, 0 notes.

## Coverage

- AC1 → T1, T2, T3
- AC2 → T1, T2, T3, T5
- AC3 → T1, T2, T3, T5
- AC4 → T1, T2, T3
- AC5 → T1, T2, T4
- AC6 → T4

## Tasks

- [x] T1: Tests first. Add `malformed_designs(d)` to `tests/testthat/helper-orchestration.R`, returning the named planted designs of AC1–AC4 built from `det_nested(d)` (each defect form at first and last position, and at all three positions for the every-position assertions), and extend `tests/testthat/test-nested-tune-grid-checks.R` with the grid driver's refusals, message-position and `conditionCall()` assertions, and the AC5 controls. Run `devtools::test()`; the new tests are red, the two existing element-class tests (`:283`, `:340`) are extended and red on the missing class.
- [x] T2: `R/checks.R`: extend `check_nested()` with `check_inner_rows()`, `check_label_columns()` and `check_label_values()`; give `check_column_class()` and the three new helpers one message shape naming every offending position or column, class `nestedtune_bad_design`, and `call = call`; confirm the race drivers' `call` reaches the condition (`R/nested-tune-race.R:275`). `devtools::test()` clean.
- [x] T3: One test block per driver in `test-nested-tune-bayes-checks.R`, `test-nested-tune-race-checks.R` (both race exports) and `test-nested-tune-sim-anneal-checks.R` over `malformed_designs(d)`, asserting class and `conditionCall()` per driver. `devtools::test()` clean.
- [x] T4: `@param resamples` at `R/nested-tune-grid.R:36` states the contract; `NEWS.md` entry; the comment at `R/parallel.R:112` re-read against the new `check_nested()`. `devtools::document()`, `devtools::test()`, `devtools::check()` clean.

- [x] T5: Review return, findings 1-6 of the [O] lens (Review section). `check_label_values()` tests missingness on `as.character()` of each label column so a factor whose `NA` is a level (`addNA()`) is refused, with a test planting `addNA(factor(c("a", "b", NA)))` as `id` at each position; a factor-labelled well-formed design joins the AC5 controls; the `weights_character_after` assertion matches "not named as rsample"; an NA-only refusal is headed by a missingness line, not the uniqueness claim; the name hint states the rule stops at `id9`, with an `id10` probe added to `malformed_designs()`; `refusal()` pins `cli.width` so the all-positions assertions do not depend on wrapping. `devtools::test()` and `devtools::check()` clean.

## Work log

- 2026-09-03: created by /milestone-plan from the candidate row "Refuse a design for more than its two columns' element classes" (M19 Out, M38 Out); the row's "inner_resamples length mismatched to nrow(resamples)" item dropped as impossible for a data frame, the rest of its remainder trimmed into the row.
- 2026-09-03: criteria audit ran in full mode ([O] fresh reader): eleven findings; nine fixed in place (fixture and positions named, probes crossed by form and position with a character non-id column added, every-position assertions over all three fixture rows, the race pair's `call`, the existing refusals gaining the class, AC5 rewritten from a skip-count comparison to named fixture constructors, the refusal count corrected to five, documentation scope settled by `@inheritParams`); two posed at the gate (the name rule against D-036; the existing refusals' shape).
- 2026-09-03: plan gate chose the reader pattern `(^id$)|(^id[1-9]$)` over rsample's constructor prefix `^id` and over no name rule because tune's and rsample's readers ignore a column outside it, so a design carrying one would be misread downstream either way; falsified by an rsample or tune release whose readers find id columns by another rule.
- 2026-09-03: plan gate chose refusing `NA` and repeated label tuples alongside the type rule over the type rule alone because repeated labels make `autoplot()` abort and `NA` labels make fold rows unattributable; falsified by a design rsample itself builds whose label tuples legitimately repeat.
- 2026-09-03: plan gate chose bringing the two existing element-class refusals to the new shape (every position, one class) over leaving them because one class lets callers catch every design refusal alike; falsified by a caller shown to depend on the old first-position message text.
- 2026-09-03: /milestone-implement started; branch `m055-design-entry-checks` cut from `main` at bd3e105 (synced with origin); question gate skipped, the plan gate having settled the three open choices.
- 2026-09-03: T1 done: `malformed_designs()` (25 planted records: the two element-class defects, the empty inner rset and the NA label each at first, last and all three rows; the repeated tuple at last and at all three; an integer `id` in place, and an integer `id2`, a character and a numeric `weights` and a list `extra` each before and after `id`; one design carrying two offending columns) and four grid-driver tests (every planting refused with the class, the call and every position; each defect named; the four whole-object refusals carry the class; five named well-formed designs pass `check_nested()` invisibly and unchanged). Red before T2: 107+ failures on the missing class and the first-only positions; the AC3 "integer `id` before and after `id`" clause read as an integer `id` in place plus an integer `id2` on either side, since one design cannot carry two `id` columns.
- 2026-09-03: catch-up: the full suite at T2 was red in `test-drift-manifest.R` alone, on the 2026-09-03 triage commit (089ff94) having compressed the mori candidate row past two figures its drift-check declaration still declares; fixed on `main` as two docs-only commits (6f189ff, 44ec8ac; the row carries the two figures and the phrase the perturbation test anchors on), merged into the branch.
- 2026-09-03: T2 done: `check_nested()` gains `is_id_name()` (the readers' pattern, cited to rsample 1.3.2 and tune 2.1.0), `check_inner_rows()`, `check_label_columns()` and `check_label_values()` (NA rows reported as such, not also as repeats; `vctrs::vec_duplicate_detect()` over the label frame); `check_column_class()` names every offending element; all ten `cli_abort()` calls carry `class = "nestedtune_bad_design"`. The race pair reaches it with `call = call` from the export's frame (`R/nested-tune-race.R:275`), unchanged. Grid checks file green; full suite green apart from the drift file above.
- 2026-09-03: T3 done: one block per driver file over `malformed_designs(d)` (the race block loops both exports through `race_call()`), asserting the class, that fitting never began, and `conditionCall()` naming the export; the three files green.
- 2026-09-03: T4 done: `@param resamples` states the contract (inherited by the four sibling pages); `NEWS.md` entry names the three new shapes, the six existing refusals now carrying the class, and every-position naming; the `R/parallel.R` fat-path comment re-read and reworded (the class-and-a-row check still passes a `manual_rset()` over another frame). `devtools::document()` no diff; `devtools::test()` 0 failures; `devtools::check()` 0 errors, 0 warnings, 0 notes (10m 14s). Status → review.
- 2026-09-03: review: draft PR #65 opened; `format-suggest` red on five `air` line-wrap suggestions in the new test code, applied by `air format` (whitespace only) and pushed.
- 2026-09-03: amendment return: AC3 — "one test per driver plants, once before `id` and once after it in column order, an integer `id2`, a character `weights`, a numeric `weights` and a list `extra`, and replaces `id` in place with an integer column, asserting the class each time" — the clause as written asks for an integer `id` both before and after `id`, which one frame cannot carry; AC1, AC2, AC4, AC5, AC6 verified and ticked; status → in-progress for the amendment alone, review stops here (steps 5-7 not run).
- 2026-09-03: criteria audit of the amended AC3 ran in full mode ([O] fresh reader, not the wording's author): no change recommended; two either-way notes (no probe pins a near-miss name such as `id10`; the clause after the semicolon binds the plant matrix, as AC1, AC2 and AC4 do), both left as they stand at the mini gate.
- 2026-09-03: amendment executed at the mini gate (user chose adopt-as-shown over a near-miss name probe and over moving the plant matrix to T1), settling review's amendment return on AC3: "one test per driver plants, once before `id` and once after it in column order, an integer `id2`, a character `weights`, a numeric `weights` and a list `extra`, and replaces `id` in place with an integer column, asserting the class each time"; `malformed_designs()` already builds that set, so no code changed. Status → review.
- 2026-09-03: review re-entered at ea1252f (route d): AC3 verified under the amended clause; fresh test() and check() clean; three-lens review. Defect return 1 of M55 at the step-7 gate (user chose the return over rejecting the finding): finding 1, a factor label whose `NA` is a level passes `check_label_values()`, fails AC2 inside its domain; findings 2-6 ride the same round as T5; AC2 unticked. Status → in-progress. Amendment-return track stands at 1 (AC3).
- 2026-09-03: T5 done (defect return 1, findings 1-6): `check_label_values()` reads each label column through `as.character()`, so a factor whose `NA` is a level is refused (planted as `label_na_level` at first, last and all three rows; red before the fix, the sentinel class reached); an NA-only refusal is headed "has a missing outer fold label", repeats alone keep the uniqueness header, both together get a combined one; the name hint states `id`, or `id2` through `id9`, with `id10` planted before and after `id`; a factor-labelled `det_nested()` joins the AC5 controls; the `weights_character_after` assertion matches "not named as rsample"; `refusal()` pins `cli.width` at 500. `malformed_designs()` now returns 29 records (24 at T1, the log's 25 miscounted). `devtools::document()` no diff; `devtools::test()` 0 failures, no skips; `devtools::check()` 0 errors, 0 warnings, 0 notes (13m 25s). Status → review.
- 2026-09-03: review re-entered at 35811e0 (route d): AC2 re-verified and ticked; fresh test() and check() clean; round-2 three-lens review, no functional defect, seven [O] findings rejected or noted at the gate. step-7 approval: PR #65 approved for merge.

## Decisions

## Review

Evidence gathered on branch `m055-design-entry-checks` at c3a2e45 (main at 1990bc9's base bd3e105 plus the two drift-fix commits, already merged; main has not moved since). Fresh runs: `devtools::document()` no diff; `devtools::test()` exit 0, summary reporter shows no failure or skip marker in any file, the five `*-checks.R` files included. Re-entered 2026-09-03 at ea1252f (only the milestone file changed since c3a2e45; main still unmoved): `document()` no diff, `devtools::test()` exit 0 clean, `cairn_validate` exit 0, `pkgdown::check_pkgdown()` no problems, README pair in sync, CI on PR #65 re-running with `format-suggest` green.

Re-entered 2026-09-03 at 35811e0 (T5, the defect-return fix; main still unmoved at 44ec8ac; branch fully pushed, PR #65 draft with CI re-running, `format-suggest` green): `document()` no diff; `devtools::test()` exit 0, no failure or skip marker in any of the 57 files, the five `*-checks.R` files included; `cairn_validate` exit 0 (18 references-staleness advisories, pre-existing); `pkgdown::check_pkgdown()` no problems; README pair in sync; no new top-level file; grep over `R/checks.R` finds the same eight `cli_abort()` sites reached from `check_nested()`, each with the class and `call = call`. AC1, AC3, AC4 and AC5 stand on that run; AC2 re-verified below; AC6 on the fresh `check()` below.

- AC1: `malformed_designs()` plants `rsample::manual_rset(list(), character(0))` at row 1, row 3 and all three rows of `det_nested(d)`; the grid test asserts class `nestedtune_bad_design` and the planted rows in the message for each; the bayes, both race, and sim-anneal blocks assert the class over every planting; the AC5 control asserts the unaltered fixture reaches the fitting sentinel unrefused. Green. Verified.
- AC2: `NA` planted at rows 1, 3 and all three; row 1's label copied to row 3 and to all rows; class asserted per driver and the rows named in the grid test ("1 and 3" for the repeat); the control asserts `repeated_design()` (`id` repeating, `(id, id2)` unique) passes `check_nested()` unchanged. Verified at c3a2e45; unticked at the gate on review finding 1 (a factor label whose `NA` is a level passed). Re-verified at 35811e0: `check_label_values()` reads each label through `as.character()`, `malformed_designs()` plants `addNA(factor(c("a","b",NA)))` as `id` at row 1, row 3 and all three rows, the grid test asserts the class and "NA" in the message for each and that an NA-only refusal is not headed by the uniqueness claim, the four sibling driver blocks assert the class over the same records, and the `repeated_design()` control still passes unchanged; fresh `devtools::test()` exit 0, no failure or skip marker in any of the 57 files. Verified.
- AC3: under the amended clause (adopted at the mini gate, ea1252f): `malformed_designs()` plants an integer `id2`, a character `weights`, a numeric `weights` and a list `extra` each once before and once after `id` in column order, and replaces `id` in place with an integer column (`helper-orchestration.R:1969-2029`); each of the five driver blocks asserts class `nestedtune_bad_design` over every planting; the grid control asserts a `nested_resamples()` design and an `rsample::nested_cv()` design carrying `id` and `id2` pass `check_nested()` invisibly. Fresh `devtools::test()` at ea1252f: exit 0, no failure or skip marker in any file. Verified.
- AC4: grep over `R/checks.R` finds ten `cli_abort()` calls reached from `check_nested()` (lines 117-157 whole-object, four; 236 class; 257 inner rows; 307 label columns; 341 label values), every one carrying `class = "nestedtune_bad_design"` and `call = call`; the grid test asserts all three rows in the message for the splits-class, inner-class, inner-empty, NA and repeat plantings at all three positions, both column names for `two_columns`, and `conditionCall()` naming `nested_tune_grid`; the bayes and sim-anneal blocks assert their own names; the race block loops both exports through `race_call()` asserting `export_name(fn)`, `call` reaching `check_nested()` from the export's frame (`R/nested-tune-race.R:275`). Green. Verified.
- AC5: the grid test names `det_nested()`, `valid_folds()`, `repeated_design()`, `nested_resamples()` and `rsample::nested_cv()` and asserts `check_nested()` returns each invisibly and identical; `devtools::test()` clean. Verified.
- AC6: `NEWS.md` names the five shapes (zero-row inner `rset`; `NA` label; repeated label; label column not character or factor; label column named outside `id`/`id` plus a digit) and the class; `@param resamples` on `nested_tune_grid()` states the contract and is inherited by the four sibling pages (`man/` regenerated, no diff); `devtools::check()` 0 errors, 0 warnings, 0 notes (8m 22s); re-run at ea1252f, 0/0/0 (11m 12s); re-run at 35811e0, 0/0/0 (11m 2s). Verified.

Consistency gate: `cairn_validate.py` exit 0 (18 references-staleness advisories, pre-existing); `document()` no diff; `pkgdown::check_pkgdown()` no problems; README.md newer than README.Rmd; no new top-level file; DESIGN.md unchanged so `cairn_impact` skipped; NEWS.md entry present naming the five shapes and the class. `format-suggest` red on five `air` wraps, fixed in c3a2e45. Re-run at 35811e0: every check above green again (validator exit 0, `document()` no diff, pkgdown clean, README in sync, no new top-level file, NEWS entry present, DESIGN.md unchanged so `cairn_impact` skipped, `check()` 0/0/0).

Independent review (three lenses, fresh context, at ea1252f). [S] blame-history: no regression of any past milestone or D-entry; one note, the whole-design id gate tightened from `^id` to the reader pattern has no near-miss-name test (`id10`), matching the criteria audit's note. [S] prior-review-record: no prior-review evidence of a regression (archived Review sections M02–M53 checked; one real inline PR comment exists, on PR #30's pkgdown workflow, unrelated). [O] diff-bug, nine findings ranked by the reviewer, each with its disposition at the gate:

1. A factor label whose `NA` is a level (`addNA(factor(c("a","b",NA)))`) passes `check_label_values()` — `is.na()` on the factor is `FALSE` while `as.character()` gives `NA` (`R/checks.R:331`). Verified against the implementation: the design returns unrefused; a plain factor with a missing value is refused. Demonstrates AC2 failing inside its domain → defect return under the return floor (first defect return on M55).
2. The factor half of the type rule has no positive test: no fixture carries a factor label column, so narrowing to character alone would fail nothing (`R/checks.R:283`; grid test `:743-780`). Fix with the return.
3. `expect_match(said("weights_character_after"), "id")` is vacuous, every column-refusal message containing "id" (`test-nested-tune-grid-checks.R:703`). Fix with the return.
4. An NA-only refusal is headed by the uniqueness claim "does not label every outer fold uniquely" (`R/checks.R:343`). Fix with the return.
5. The hint "rsample names them `id`, `id2` and so on" is shown on the `id10` refusal the rule just rejected (`R/checks.R:313`). Fix with the return, with an `id10` probe (the near-miss note above).
6. The all-positions assertions match the literal "1, 2, and 3", width-sensitive under cli wrapping (`test-nested-tune-grid-checks.R:665`). Fix with the return (`cli.width` pinned in the helper).
7. `check_column_class()` repeats the type once per element ("a number, a number, and a number"). Cosmetic; rejected at triage (style).
8. `refusal()` duplicates `grid_refusal()` in the same test file. Rejected at triage (style).
9. Tracking counts: `malformed_designs()` returns 24 records where the T1 log says 25; eight distinct `cli_abort()` sites are reached from `check_nested()` (the shared class check counted once per caller made "ten" above); NEWS's "three new shapes" is accurate (three new, six existing gaining the class). Docs only; corrected here, no code change.

Verified correct by [O]: `is_id_name()` byte-identical to rsample 1.3.2 and tune 2.1.0's `col_starts_with_id`; check ordering sound; zero-row and label-less designs caught by the whole-object refusals; `call` reaches the condition from every export; no rsample constructor produces a name the rule refuses (D-047's falsifier).

Independent review, round 2 (three lenses, fresh context, at 35811e0). [S] prior-review-record: no prior-review evidence of a regression; each of round-1 findings 1-6 confirmed fixed against the code (archived Review sections M19 and M38 the only relevant hits, neither contradicted). [S] blame-history: no regression; the every-offender shape supersedes M19's first-offender implementation choice by the plan-gate decision, the `R/parallel.R` comment keeps the M50 fat-path claim, `is_id_name()` matches installed rsample 1.3.2, D-036 binds the results class and D-047 carves out the entry check; its one note is the AC2 box, ticked this round. [O] diff-bug, no functional defect; seven findings ranked by the reviewer, each with its disposition at the gate:

1. Two label columns sharing one name (a hand-built frame with `id` twice, the second integer) pass every label check: `setdiff()` on the names is unique and `resamples[[col]]` reads the first (`R/checks.R:135`); verified by probe; unreachable from `data.frame()` or any rsample constructor. Rejected at the gate: outside the contract's domain (a frame with repeated names is not a data frame `data.frame()` or rsample builds).
2. The column-name assertion for the `id_integer`, `id2_integer_before` and `id2_integer_after` records is vacuous, the hint bullet always containing `id2` (`test-nested-tune-grid-checks.R:673`); the sibling "integer" assertion carries the weight. Rejected at the gate: nothing untested.
3. An `NA` label refusal names the row but not the column holding the `NA` (`R/checks.R:341-346`); AC4's letter met. Rejected at the gate: cosmetic.
4. `check_column_class()` repeats the type once per element; round-1 finding 7, rejected as style. Rejected again.
5. `refusal()` duplicates `grid_refusal()` and is copied into the bayes, race and sim-anneal files; round-1 finding 8, rejected as style. Rejected again.
6. AC2 unticked in the milestone file; tracking, ticked this round. Noted.
7. The `_first` position assertions match "1" alone; the `_last` and `_all` records supply the discrimination. Noted.

Verified correct by [O]: no false refusal over `nested_cv()` on `vfold_cv`, repeated `vfold_cv`, `mc_cv`, `group_vfold_cv` (plain and repeated), `clustering_cv`, `sliding_window` and `rolling_origin`; single-row designs, factor `id`, and factors with unused levels pass; each T5 fix probed; `cli.width` pin effective at condition construction; check ordering sound; `R/parallel.R` comment-only; NEWS and `@param resamples` match the code.

conversation: PR #65 — empty read (no reviews, no comments, no unresolved threads) immediately before the round-2 merge chip.
