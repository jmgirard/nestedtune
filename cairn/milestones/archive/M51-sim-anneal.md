# M51: `nested_tune_sim_anneal()` runs finetune's simulated annealing inside the outer loop

**Status:** done (2026-09-03, PR #61 https://github.com/tidymodels/nestedtune/pull/61; resolves #35 closes)

**Goal:** A user runs nested cross-validation around `finetune::tune_sim_anneal()` through `nested_tune_sim_anneal()`, a sibling of `nested_tune_bayes()`, and each outer fold's `.inner_metrics` carries the search's iterations.

**Outcome:** `nested_tune_sim_anneal()` (`R/nested-tune-sim-anneal.R`) on `nested_tune_bayes()`'s shape with `iter = 10` and `initial = 1` as its own arguments and a `finetune::control_sim_anneal()` through `...` (D-042); the `tune_sim_anneal` entry in `tuner_registry` (`R/tuner.R`: finetune, `control_sim_anneal`, `takes_grid = FALSE`, `iterates = TRUE`, label "simulated annealing") and `tuner_anneal(iter, initial)`; `check_iter()` and `check_initial()` take the calling sibling's floor (Bayes 0 and 2, annealing 1 and 1); `empty_inner_metrics()`, `candidate_set()`, `procedure_counts()` and `procedure_label()` read `tuner_iterates()` and the registry label, so `.iter` rides the fold record and its zero-row prototype and the final-fit print takes one shape for both iterating tuners; `nested_final_fit()` needed no code — `procedure_tuner()` and `run_tuner()` rebuild the run from the registry; `?nested_tune_sim_anneal` with the six-heading `control_sim_anneal()` classification (18 slots on finetune 1.3.0: `allow_par` Forced, `time_limit` and `verbose_iter` Passed through under caveats, `save_history` Not returned) and a by-hand recipe; the `time_limit` known issue in DESIGN. Reference loops `reference_nested_anneal_loop()` and `reference_anneal_final_fit()`, the AC3 refusal probes behind a `dispatch_folds` sentinel, the RNG battery, BC13 daemon identities and a pin test on finetune iterating at `iter = 0` in `tests/testthat/`.

**Decisions:** D-046 (`initial` floor 1 and `iter` floor 1, departing from D-040's `iter` 0 because finetune 1.3.0 loops `1:0` at `iter = 0`); the plan's `iter = 0` choice was superseded by a gated amendment at the implement question gate. Milestone-local: none.

**Review:** three-lens fan-out, one round with a fix-now batch; zero defect returns, zero amendment returns. [S] blame-history clean; [S] prior-review one finding (DESIGN's registry paragraph still keyed on the Bayesian name), fixed. [O] diff-bug ten, no code bug: eight fixed — DESIGN enumerations missing the export and `tuner_anneal()`, a pin test for D-046's falsifier, the `tuner_iterates()` spelling in `empty_inner_metrics()`, the final-fit `initial` count bounded rather than pinned, the check-before-dots ordering commented, the some-fold-iterated guard on the metric-separating oracle; two follow-up to one candidate row (the `?nested_final_fit` recipe has no racing or annealing branch; finetune's `>= 1.0.1` floor against the pages classifying `workflow_size`); two rejected (AC5's `attr(x, "procedure")` holds read literally; help-page sentences about finetune behaviour lie outside the test doctrine). At step 8 the CI watch reached the harness ceiling twice, the second time because pushing the approval line restarted the matrix; merged on nine green check legs with `test-coverage` cancelled at its 20-minute job cap (19m24s on the pre-fix head) — non-gating per PROFILE, recorded on the M34 wall-clock row and as a lesson. Nothing graduated or retired.
