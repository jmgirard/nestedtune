# M62: A vignette reads the results object: summary, agreement, both plots, a failed fold, dplyr and the survival arguments

**Status:** done (2026-09-04, PR #72 https://github.com/tidymodels/nestedtune/pull/72)

**Goal:** Ship `vignettes/results.Rmd` ("Reading the results"), which walks the `nested_results` object's columns, `summary()`, `collect_metrics(summarize = FALSE)`, `agreement()`, both `autoplot()` types, a run with one failed fold and the partial-summary warning that follows, the dplyr verbs that keep the class and the ones that shed it, and `event_level` and `eval_time` on a censored-regression run.

**Outcome:** `vignettes/results.Rmd` guards on `ranger` (one notice, then `knit_exit()`), reruns the guide's design, and gives each column (`splits`, `id`, `.metrics`, `.selected`, `.inner_metrics`, `.notes`, `.completed`, the two seed columns) one sentence and an executed peek, then `print()`, `summary()`, `collect_metrics()` both ways, `agreement()` and both `autoplot()` views with `fig.alt`. The failed-fold section plants `recipes::check_range(hp)` on the guide's run: Fold1 fails at the outer fit (the Maserati's 335 hp lies above the range that fold trained on), the chunk asserts `sum(!failed$.completed) == 1L`, the fold's `.notes` print `location`, `type` and `note`, and `summary()` under `withCallingHandlers()` shows class `nestedtune_partial_summary` with its 4-of-5 message and the summary over the four. The dplyr section prints `class()` after `mutate()` (kept), `filter()` dropping a fold (shed) and `res[, "id"]` (shed) with the help page's rule in one sentence, base `[` named as the same door. The survival section, under `has_srv` over `censored` and `survival`, simulates the test suite's `srv_data()` shape (180 rows, seed 51), runs `survival_reg(dist = tune())` with `eval_time = c(0.5, 10)` and `brier_survival`, shows `.eval_time` in both metric tables with both times read inline from `results = "asis"` chunks, and `agreement()` on it; a censored-masked build keeps heading, intro and one notice. `results` under Guides in `_pkgdown.yml`; NEWS entry. Page alone 9.7 s median against 60, all four vignettes 41.8 s against 150; two masked builds as AC4 states; citation guard 37 expectations green; check 0/0/0.

**Decisions:** none cross-cutting. Milestone-local: a data-keyed failing fold asserted to be exactly one over the test suite's design-level plant, because a page teaches with a workflow a reader could write; `check_range(hp)` over `check_new_values(carb)`, which failed two folds; the `recipes` guard dropped by mini-gate amendment since tune imports recipes, so a recipes-masked build fails at `library(nestedtune)` before any guard runs (M61's dials shape); AC4's masked disjunction names `censored` alone since `survival` sits in the system library and cannot be masked from `.libPaths()`.

**Review:** three-lens fan-out; [O] 16 findings, [S] blame-history none, [S] prior-review three (P1–P3, the M06 F5 shape: a behavior claim with no executed chunk behind it). Fixed on the branch: the failed-run print in its own unmuted chunk (O1); survival chunks under `include = has_srv` with prose in guarded `asis` chunks (O2, O16); the completed-fold note prose (O3); heading and intro naming `event_level` and `eval_time` (O4); "or overwrites" in the rule (O5); seeds pointing to the guide and help pages (O6); "a plain tibble print" (O7); "less `.config`" (O8); the outer fold id printed beside the inner id (O9); "beyond a small slack" (O10); base `[` named (O12); `n` and `prop` named (O13); "Every tuning driver" with `nested_final_fit()` reading seeds back (O15); `agreement(surv_res)` shown (P3). Rejected: O11 (`survival` is recommended, so absent-able; the guard is free), O14 (a peek would repeat the per-fold table), P1 and P2 (verified against the code; an all-failed run is a second run on a page at length). PR conversation empty at three gate reads. 11 CI checks green on head bf3d093; two ceiling stops, each preceded by a pushed docs-only commit that restarted the matrix against the M50 lesson. Nothing graduated.
