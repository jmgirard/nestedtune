# M50: `nested_tune_race_anova()` and `nested_tune_race_win_loss()` run finetune's racing tuners inside the outer loop

**Status:** done (2026-09-02, PR #60 https://github.com/tidymodels/nestedtune/pull/60; resolves #35 partial)

**Goal:** A user runs nested cross-validation around finetune's two racing tuners through two sibling exports of `nested_tune_grid()`, and each outer fold records every candidate its race scored and how many inner resamples scored it.

**Outcome:** `nested_tune_race_anova()` and `nested_tune_race_win_loss()` (`R/nested-tune-race.R`) over one internal `nested_tune_race()`, with `nested_tune_grid()`'s formals and a `finetune::control_race()` through `...` (D-042); `tuner_registry` in `R/tuner.R` (package, `requires`, control constructor and class, `takes_grid`, `iterates`, label) replaces the `switch()`es in `default_control()`, `control_class()`, the zero-row prototype, the final fit's grid re-check and `procedure_label()`, and `run_tuner()` calls through the registry's namespace; `collect_inner_metrics()` is the one `collect_metrics()` call behind `inner_metrics()` and `scored_candidates()`, `all_configs = TRUE` on a `tune_race`; entry refusals `check_tuner_installed()` (`nestedtune_pkg_not_installed`, also called by `nested_final_fit()`) and `check_race_burn_in()` (`nestedtune_bad_burn_in`), `check_control()` naming the control's package; `attach_daemon_pkgs()` attaches the registry's `requires` beside the workflow's packages; the shared help page `?nested_tune_race` with the six-heading control classification and a by-hand recipe; `finetune (>= 1.0.1)`, `lme4`, `BradleyTerry2` in Suggests. Reference loops `reference_nested_race_loop()` and `reference_race_final_fit()`, racing fixtures, BC12 daemon identities, and the RNG battery in `tests/testthat/`.

**Decisions:** D-044 (the three Suggests and the entry refusal), D-045 (the finetune floor, written at review). Milestone-local: none.

**Review:** three-lens fan-out, one round with a fix-now batch. [S] blame-history and [S] prior-review lenses clean. [O] diff-bug: twelve findings; eight fixed before the merge at the user's choice — no finetune floor (D-045), the final fit skipping the package check, the daemon attach reading `package` where D-044 says `requires`, a two-package install hint that was not valid R, `tuner_entry(NULL)`'s opaque error, refusal and roxygen wording naming only grid and Bayes, NEWS's "first `burn_in`" with `randomize` unmentioned, a DESIGN sentence stronger than the code; four rejected — elimination assertions resting on one small race (AC3 and AC6 ask for it), the unconditional drop of `tune` from the daemon attach (intentional), an unused test-helper parameter, and a 12-seed AC2 identity observation. `format-suggest` CI failed on four new test files, formatted at review. The CI watch outran the harness ceiling because every push to the PR restarts its matrix; the moved watcher was kept as the one watcher and acted on at its completion, a logged deviation from the stop-at-ceiling step. Nothing graduated or retired.
