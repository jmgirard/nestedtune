# M52: The test suite runs its files in parallel and fits its CI caps with headroom

**Status:** done (2026-09-03, PR #62 https://github.com/tidymodels/nestedtune/pull/62)

**Goal:** The suite's CI legs finish well inside their caps by running test files in parallel, with the hang trace and the daemon guards intact.

**Outcome:** `Config/testthat/parallel: true` and an eleven-file `Config/testthat/start-first` list in `DESCRIPTION` (the ten files over 50 s serially plus `test-nested-final-fit-oracles.R`, slow under covr); `HangTraceReporter` (`tests/testthat/helper-hang-trace.R`) declares `parallel_support` and `parallel_updates`, prints each file's and block's `start` on first sight and its `end` once under testthat's live-update loop, which re-announces the file and block before every forwarded event; `check_reporter_with_hang_trace()` in `tests/testthat.R` builds the runner's composite and sets `parallel_updates` on it, since testthat reads both capabilities off the reporter it is handed; `test-hang-trace.R` asserts both declarations and one live pair per file and block under a two-worker run; `TESTTHAT_CPUS` in both gating workflows' job `env:` (4 on ubuntu and windows, 3 on macOS, via an os ternary in `R-CMD-check.yaml`; 4 on the covr job); `benchmarks/profile-tests.R` sets `TESTTHAT_PARALLEL=FALSE`; the PROFILE test-doctrine slot states the setting. Measured on the merged code across three CI attempts: `Test coverage` step 9.87–11.23 min (was 17m10s), check legs 11.6–16.7 min (windows was 26m56s); local `devtools::test()` at four workers 140 s against 1397 s serial. Codecov total unchanged at 97.51%; no elapsed bound needed revisiting; no new skips.

**Decisions:** milestone-local: the hang trace runs in live-update mode, not the plan's burst replay, because burst replay stamps a finished file's lines together and prints nothing for a file that hangs. No D-entry.

**Review:** three-lens fan-out; one defect return (AC1 read 722 s against a 720 s bar on a single attempt), then an amendment round at the implement gate: AC1 and AC2 read the median of three attempts and AC3 defines the measured head as a pushed commit from which the merge-time branch head differs only under `cairn/`, replacing an unmeasurable "final head"; the audit readers ran on Sonnet (Opus 529-overloaded on five spawns). [O] diff-bug nine findings, none a code bug: one fixed in PROFILE prose (testthat prefers `getOption("Ncpus")` over `TESTTHAT_CPUS`); seven to one candidate row (duplicate test descriptions collapse to one trace pair; teardown `cat()` output is dropped under parallel files; nothing validates `start-first` names; the fixture cache is per worker; `open` never pruned at `end_file`; the trace test leaves its tempdir; `R-CMD-check-hard.yaml` has no `TESTTHAT_CPUS` or cap); one rejected (line length). [S] blame-history two: PROFILE's scaffold comment clause restored; the M48 windows range rejected as superseded. [S] prior-review clean. The M51 covr-cap lesson corrected in place; nothing graduated or retired.
