# M61: A vignette runs the four alternative inner tuners on one design and shows what each records

**Status:** done (2026-09-04, PR #71 https://github.com/tidymodels/nestedtune/pull/71)

**Goal:** Ship `vignettes/tuners.Rmd` ("Choosing the inner tuner"), which runs `nested_tune_bayes()`, `nested_tune_race_anova()`, `nested_tune_race_win_loss()` and `nested_tune_sim_anneal()` on the getting-started guide's design and workflow, shows what each fold's `.inner_metrics` records for each, shows tune's control reaching the inner call through `...`, and says what differs from calling tune or finetune directly.

**Outcome:** `vignettes/tuners.Rmd` copies the guide's design and workflow chunk unchanged, builds a `param_info` (`mtry` 2–8, `min_n` bounded to the grid's range) the Bayesian and annealing tuners need, then runs each of the four tuners with its cost arithmetic as inline R. It prints fold 1's `.inner_metrics` for the Bayesian (`initial = 4, iter = 3`) and annealing (`initial = 3, iter = 3`) runs with the `.iter` range read inline, and the ANOVA race's fewest-fits fold (Fold3, 23 of 30 fits, four of six candidates below the inner count of 5) with the eliminated count read inline; `fits` is named by fold id. A control section passes `control_bayes(verbose = FALSE, no_improve = 3)` through `...`, prints the `procedure` record, reads `allow_par` inline and states the `event_level` rule and the dropped `seed` as text; a differences section covers `save_pred`, `save_workflow`, `extract` and `save_history`, seeding at entry, and entry-time refusals. Guards: `knit_exit()` on `ranger`; racing sections built only with `finetune`, `lme4` and `BradleyTerry2`, annealing with `finetune`, one notice chunk (`eval = !has_race`) naming the missing packages and whether annealing is skipped; guarded figures emitted by `results = "asis"` chunks since inline R evaluates under a skipped chunk. `tuners` under Guides in `_pkgdown.yml`; NEWS entry. Build 31.6 s median of three against the 45 s cap; four masked builds behaved as AC4 states; citation guard green; check 0/0/0.

**Decisions:** none cross-cutting. Milestone-local: a CRAN vignette under a 45 s cap over a site-only article (plan gate); the `dials` guard dropped by mini-gate amendment, since tune imports dials (D-029) and a dials-masked build fails at `library()` before any guard runs; `min_n` bounded because dials' default upper bound of 40 exceeds a 20-row inner analysis set (ranger warned on every fold, `rsq` `NaN`).

**Review:** three-lens fan-out; [O] 12 findings, [S] blame-history none, [S] prior-review none (one real GitHub thread exists, on PR #30 against an untouched file). Fixed on the branch: NEWS misnaming annealing's requirements (F1); "optimisation" spelling (F3); entry refusals scoped to racing only (F5); "none of the drivers has `control`" scoped to the page's four (F6); `save_history` added to the not-returned list (F7); seeds drawn at entry, not per fold (F8 first half); `missing`/`anova` renamed `absent`/`race` (F9); `fits` named by fold (F10). Rejected: F2 (criteria pending, not failing), F4 (a second notice contradicts AC4), F8's parallel proviso (M63's scope), F11 (hardcoded `rmse` reads better here), F12 (tune imports dials). PR conversation empty at three gate reads. 11 CI checks green on head 4d4b14c; two earlier waits were cancellations, not slow runs — each pushed ceiling-log commit re-triggered the `pull_request` matrix and `cancel-in-progress` killed the run (the M50 lesson, not followed at those resumes); the approval line was pushed after green and the PR merged at once. Nothing graduated; the M19 case-weights lesson pruned for cap room, its mistake now covered by `test-nested-tune-grid-checks.R`.
