# M56: The results class, the extract defaults and the final-fit page close the M37, M34 and M51 review findings

**Status:** done (2026-09-03, PR #66 https://github.com/tidymodels/nestedtune/pull/66)

**Goal:** Close the review findings on `R/nested-results.R`, the two `extract_` default methods and the final-fit and finetune help pages that the M37, M34 and M51 reviews deferred.

**Outcome:** `print.nested_results()` returns after the rows when `has_results_columns()` is false, so the columnless token `vctrs::vec_cbind_frame_ptype()` hands back prints the banner and a `× 0` header and no longer errors on `.completed`. `duplicated_record_names()` (`R/nested-results.R`) counts a duplicate over the record's names alone, and the three doors — `can_reconstruct_results()` (the `bind_cols()` door), `vec_restore.nested_results()` and `names<-.nested_results()` — shed the class and every attribute `results_attributes()` and `template_attributes()` name when a record name is duplicated or a record column renamed, and keep the object on a clash between two caller-added columns. `names<-.nested_results()` decides on the names alone. The `extract_tune_results()` and `extract_scored_candidates()` defaults abort with `nestedtune_no_extract_method` before `check_dots_empty()`; `test-dots-barrier.R` names them in `NO_METHOD_DEFAULTS`. The `?nested_final_fit` reproducibility recipe carries racing and annealing branches, ordered grid, racing, annealing, Bayesian so `control$seed` sits below every branch that uses the recorded control untouched; the racing and annealing pages say the `workflow_size` rule was read on finetune 1.3.0. Two NEWS entries. Tests: the token print shape, the duplicate shed through three doors, the non-record clash kept through three doors, a rename loop over the nine record columns, the no-method class per generic, and an Rd test in `test-tuner-registry.R` asserting each `tuner_registry` name is called in the recipe's one code block. Suite 5736 passes, no skips; `check()` 0/0/0.

**Decisions:** plan gate: the finetune version stated in the two pages rather than a floor bump; the `vec_cast` re-templating dropped (IP4) and the `rbind` one dropped (no observable change), both to the trimmed M37 row. Milestone-local: the duplicate shed is a second fault D-035's "one thing" did not anticipate, and the rename rule reads names alone because base `names<-` never moves a value. Cross-cutting: D-048, narrowing D-032 and annotating D-035.

**Review:** three-lens fan-out, two rounds. Round 1 at 7fa11f0's parent: [O] F1–F10, [S] H1–H2, [S] prior-review clean; F1 confirmed by command (a clash between two non-record columns shed the record, inside AC3's domain) — defect return 1, F1, F2, F3, F4, F7, F9 and F10/H1 fixed as T8–T12; F5 (latent gate), F6 (the token's row count, IP4), F8 (registry names equal the tuner fields) and H2 (the two dots exemptions, intentional and tested) rejected. Round 2 at 7fa11f0: both [S] lenses clean, every fix confirmed; [O] G1–G4, none a code defect — G1 the tick read before its commit, G2 D-032 unannotated (landed as D-048 at hygiene), G3 H2's shape, G4 F5 again; one bot air-reflow thread fixed now after the `format-suggest` check went red on it. Defect-return count 1, amendment-return count 0. The CI watch hit the session ceiling once; merged on the re-posed approval at the green head. Nothing graduated or retired.
