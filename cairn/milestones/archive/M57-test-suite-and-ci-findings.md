# M57: The hang trace, the parallel-files setup and two test files close the M52, M37 and M34 review findings

**Status:** done (2026-09-03, PR #67 https://github.com/tidymodels/nestedtune/pull/67)

**Goal:** Close the review findings on the hang trace, the parallel-files setup, `test-vctrs-compat.R` and `test-dots-barrier.R` that the M52, M37 and M34 reviews deferred.

**Outcome:** `helper-test-source.R` parses a file's top-level `test_that()` calls (`test_that_calls()`, `test_that_descriptions()`); `test-hang-trace.R` scans every `test-*.R` for a duplicated description and `test-parallel-detection.R` resolves every `Config/testthat/start-first` name to a file, each with a planted-defect test. `HangTraceReporter$end_file()` prunes `open` and `seen` through one `forget()`; `trace_lines_parallel()` unlinks its directory on exit and `fixture_two_blocks()` registers the unlink on its caller's frame. The fixture-cache report moved into `print_fixture_cache_report(report, file = stderr())` in `helper-orchestration.R`, which the teardown calls with the stream spelled out; the comments say the cache is per worker process and that under parallel files a worker's teardown output reaches no log on either stream (measured, testthat 3.3.2), so the report is a serial-run report. `R-CMD-check-hard.yaml` caps its job at 30 minutes (measured run 12m09s) and sets `TESTTHAT_CPUS: 4`; PROFILE names the capped workflows and the three that set the worker count. Eleven `tibble::`-calling blocks in `test-vctrs-compat.R` open with the tibble skip, checked by a parse walk. `DOTS_PROBED_METHODS` names seven methods the dots-barrier registry probe must reach; the `collect_metrics(` source scan, its filters and its discrimination test are gone, replaced by a runtime test that `collect_metrics(res, FALSE)` raises `rlib_error_dots_nonempty` while `summarize = FALSE` reaches the unsummarised shape. Two minor amendments at implement: eleven tibble blocks rather than the plan's nine (M56 added two), and the positional-argument runtime test the plan assumed existed was added.

**Decisions:** none (the plan gate's two choices, removing the source scan and taking the cheap answers over a reporter counter and a shared disk cache, are milestone-local and their promotion conditions live in the M52-leftovers candidate row).

**Review:** three-lens fan-out; [O] 11 findings, [S] blame-history 2, [S] prior-review 0. Fixed on the branch: a comment citing a nonexistent `test-collect-metrics.R`; the tibble-skip comment's false premise (tibble is an Import of dplyr and tune, so the no-Suggests leg has it and the skip never fires there); the new runtime block's unseeded fixture request (now `set.seed(2)`, one build for 22 requests) and its recipes-only skip; PROFILE's "both gating workflows" for `TESTTHAT_CPUS`; the yaml comment's "on main" for a pull_request run; the open-pruning test's precondition counted on the wrong reporter. Rejected: top-level-only parsing (every block today), first-position skip check (the criterion's wording), the stderr calls living in the helper, and the lost vignette-site coverage of the removed scan (the plan gate's choice, carried by the candidate row). `devtools::test()` 5756 passing; `devtools::check()` 0/0/0; 11 CI checks green. Nothing graduated or retired.
