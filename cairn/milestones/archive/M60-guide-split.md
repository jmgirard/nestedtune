# M60: The guide splits into a getting-started path and a page on what the estimate means, and the citation guard sweeps every page under vignettes/

**Status:** done (2026-09-04, PR #70 https://github.com/tidymodels/nestedtune/pull/70)

**Goal:** Split `vignettes/nested-cv.Rmd` into a short getting-started guide that walks the design → loop → report → final-fit → reproducibility → write-up path and a code-free concept page carrying the M25 material on what the estimate means, and make the citation guard read every page under `vignettes/` rather than one hardwired path.

**Outcome:** `vignettes/nested-cv.Rmd` keeps the design, grid, loop, report, fold choices with both `autoplot()` figures, the final fit with its comparison and two refusals, a shortened Reproducibility section (the `args()` chunk dropped) and the write-up; each moved passage is one paragraph plus a `vignette("estimate")` link, and no citation or References section remains. New `vignettes/estimate.Rmd` ("What the estimate means") runs a `setup` chunk only and carries the estimand, the pessimism figures, `std_err`, the no-comparison warning, the disagreement mechanism, "When this is worth the cost", the feature-selection caveat and the six References the guide listed. `tests/testthat/test-vignette-citations.R` enumerates every `.Rmd` under `vignettes/` recursively, applies its rules per page as one `expect_<rule>()` each (at most one References heading and at least one entry under it; citations listed; entries parse, are backed by a shelf page naming surname and year, and are cited), adds the prose-numeral rule (after YAML, fences, every backtick span and References are removed, a paragraph or list item with a digit carries a citation), and shows each rule red on planted fixtures in a temp tree with a one-page shelf; the real-tree tests skip out of tree. `estimate` under Guides in `_pkgdown.yml`; `^vignettes/articles$` in `.Rbuildignore`; README links both pages by site URL; NEWS entry. Vignette build 5.5 s median of three.

**Decisions:** none cross-cutting. Plan-gate choices, milestone-local: split the guide before writing M61–M64's pages; a code-free concept page over keeping the literature beside the worked example; the fixture tempdir on base `tempfile()` with a caller-frame `on.exit()` since withr is not a dependency.

**Review:** three-lens fan-out; [O] 8 findings, [S] blame-history 1, [S] prior-review none. Fixed on the branch: the numeral fixtures' YAML now carries a digit (O1); an emptied References section passed and a last-line heading made `seq()` run backwards, now rule 1b with a fixture and `seq_len()` (O2); "linear" dropped from the SVM figure the shelf does not qualify (O3); `res` introduced on the concept page (O4); the guide's duplicate section title retitled "Where this example sits" (O5). Resolved by the evidence: O6. Rejected: O7 (the real-tree half runs only under `devtools::test()`, disclosed since M25), O8 (a line under a list item is a lazy continuation in markdown), S1 (the dropped `args()` chunk was the planned trim; the seed contract is documented and tested on the help pages). `devtools::test()` and `check()` 0/0/0; 11 CI checks green. One CI wait hit the foreground ceiling; the PR merged as pushed on its green head, so the ceiling log line lives only here. Nothing graduated or retired.
