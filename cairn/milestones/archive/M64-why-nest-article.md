# M64: A site-only article repeats a null-data simulation showing tuned-CV optimism and the nested estimate removing it

**Status:** done (2026-09-05, PR #74 https://github.com/tidymodels/nestedtune/pull/74)

**Goal:** Ship `vignettes/articles/why-nest.Rmd` ("Why nest: a simulation"), built by pkgdown from a stored result that `vignettes/articles/why-nest-sim.R` produces, showing over repeated draws of pure-noise wide data that the best tuned cross-validation score sits above the known 50% accuracy while the nested estimate sits at it.

**Outcome:** `vignettes/articles/why-nest-sim.R` (n = 60, p = 200 Gaussian features, a fair-coin label, `parsnip::mlp()` on nnet over a 20-candidate `hidden_units` × `penalty` grid, 30 seeded replicates, flat `tune_grid()` best accuracy against `nested_tune_grid()`'s estimate; stops on a dropped outer fold; records the pre-run commit, `-dirty` on an unclean tree) writes `vignettes/articles/why-nest.rds` carrying the design, seed 20260905, null accuracy 0.5, tolerance 0.05, commit and date. The article reads every figure from the store: median flat best 0.567 against median nested 0.458 (0.067 above the null against 0.042 below); the nested mean 0.485 against a standard error of 0.016, the below-null median derived from independent labels rather than composed; the chance a flat best lands at the null stated as one in `2^K`. One-panel figure of paired points with median crossbars and a dashed null line, `fig.alt` set. `_pkgdown.yml` entry, NEWS entry, `nnet` out of Suggests (D-051), no `vignettes/articles/` path in the built tarball; a fresh-process render leaves nnet, tune and nestedtune unloaded. A run takes 1888–4108 s.

**Decisions:** D-050 then D-051 (`nnet` into, then out of, Suggests). RB06/RR06: AC5's masked build was unsatisfiable (nnet is a recommended package in the system library and sits in tune's import closure through recipes and ipred), reworded to the fresh-process render check with the tarball listing split out as AC8. Milestone-local: a repeatable measurement stays in the criterion with its instrument named, its command and output in the task and the review evidence; the original AC5 was never ticked under any reading.

**Review:** three-lens fan-out. [O] 17 findings: 15 fixed (the below-null explanation and its contradiction rewritten from the store, the `2^K` sentence, `abs()` on the flat distance, replicate counts above/at/below, the fold-failure stop and pre-run commit in the script, `position_jitter(seed =)`), O14 fixed in part (`MaxNWts` in the design table; the `v_outer` coupling rejected), O15 rejected (no serial assertion: IP2 promises worker-count invariance). [S] blame-history 2 notes (B1 the LESSONS line, landed at hygiene; B2 rejected, no D-050 collision). [S] prior-review: no evidence. PR conversation empty. CI green on 686e146 and c72ad94 after two ceiling stops; one held local tracking commit (the second stop's log line) carried into the hygiene commit. Hygiene: the M06 lesson extended with the Import-closure and `Priority` check, absorbing M57's overlapping clause; nothing retired or graduated.
