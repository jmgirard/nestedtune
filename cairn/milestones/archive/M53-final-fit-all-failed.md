# M53: `nested_final_fit()` refuses a results object whose every outer fold failed

**Status:** done (2026-09-03, PR #63 https://github.com/tidymodels/nestedtune/pull/63)

**Goal:** `nested_final_fit()` refuses a `nested_results` in which no outer fold completed, so a model is never returned whose companion estimate does not exist — a misuse GP3 refuses, since IP3 makes that estimate the number the model is reported with.

**Outcome:** `check_completed_folds()` (`R/checks.R`), called from `nested_final_fit()` right after `check_results_record()` and before the tuner, grid and seed steps, reads `results$.completed` and aborts with class `nestedtune_no_completed_folds`, `call` threaded, the message naming `summary()`; `check_any_completed()` (`R/nested-results.R`) carries the same class, so `collect_metrics()`, `autoplot()` and `agreement()` on an all-failed run signal one class; the four help pages name it; one NEWS entry. Tests: the refusal at both `break_every_fold()` stages, `.Random.seed` identical across the refused call, a one-failed-one-completed control fitted, the record refusals firing before the all-failed one, and each door's all-failed test asserting the class. M46's read-nothing-but-splits probe exempts `.completed`. Suite 4813 pass; `check()` 0 errors, 0 warnings, 0 notes.

**Decisions:** milestone-local: the final fit reads `.completed` from the fold rows and M46's probe exempts that column, since the column travels with the rows and the stamped `folds_completed` count is a copy of it. Plan gate: one shared class on all four doors; a partial run's final fit stays silent; a new class rather than `nestedtune_bad_results`. No D-entry.

**Review:** three-lens fan-out, no defect return. [O] seven findings: O3 fixed (DESIGN's final-fit paragraph names the fourth refusal), O4 fixed (a test pins the record refusals firing first), O5 fixed (unreachable probe branch removed); O1 and O2 (`.completed` NA or missing dies with a base R error rather than a classed refusal) routed at hygiene to DESIGN Known issues together with the malformed-object candidate row's M38 and M39 findings, the row trimmed to its design-refusal head; O6 rejected (pre-existing link style), O7 not a finding. [S] blame-history one finding rejected (the probe narrowing is the milestone's decision); [S] prior-review clean. The pull_request check matrix restarted on each approval push, three ceiling stops; merged at the green head, two docs-only work-log commits carried to main by cherry-pick after the squash. The M50 push-restarts-CI lesson extended; nothing graduated or retired.
