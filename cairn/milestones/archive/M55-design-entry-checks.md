# M55: Every driver refuses a design whose inner resamples are empty, whose fold labels do not uniquely name its folds, or whose columns are not an rsample design's

**Status:** done (2026-09-03, PR #65 https://github.com/tidymodels/nestedtune/pull/65)

**Goal:** The five orchestrators refuse, at the call the user wrote and before any fold runs, a nested design whose inner `rset` has no rows, whose fold labels hold an `NA` or repeat across rows, or whose label columns are not what rsample's readers would find — naming every offending position.

**Outcome:** `check_nested()` (`R/checks.R`) gains `is_id_name()` (the pattern `(^id$)|(^id[1-9]$)`, byte-identical to rsample 1.3.2's and tune 2.1.0's `col_starts_with_id`), `check_inner_rows()` (an `inner_resamples` element that is an `rset` with zero rows), `check_label_columns()` (a column beside `splits` and `inner_resamples` that is not character or factor, or not named by the pattern) and `check_label_values()` (an `NA` label or a label tuple two rows share, labels read through `as.character()` so a factor whose `NA` is a level is refused; `vctrs::vec_duplicate_detect()` over the label frame; an NA-only refusal headed by missingness). `check_column_class()` names every offending element. Every one of the eight `cli_abort()` sites reached from `check_nested()` carries `class = "nestedtune_bad_design"` and the exported driver's `call` (the race pair from the export's frame). `@param resamples` on `nested_tune_grid()` states the contract, inherited by the four sibling pages; one NEWS entry; the `R/parallel.R` fat-path comment reworded (a `manual_rset()` over another frame is still admitted). Tests: `malformed_designs()` in `helper-orchestration.R` (29 planted records: each defect at first, last and all three rows of `det_nested(d)`, columns before and after `id`, an `id10` near-miss, two offending columns at once), a grid block asserting class, call and every position, one block per other driver, and named well-formed controls (`det_nested()`, a factor-labelled copy, `valid_folds()`, `repeated_design()`, `nested_resamples()`, `rsample::nested_cv()`) returning invisibly. Suite clean, no skips; `check()` 0 errors, 0 warnings, 0 notes. D-047 records the contract.

**Decisions:** plan gate: the readers' pattern over rsample's constructor prefix `^id` and over no name rule; `NA` and repeated tuples refused alongside the type rule; the two existing element-class refusals brought to the every-position, one-class shape. Cross-cutting: D-047, annotating D-036.

**Review:** three-lens fan-out, two rounds. Round 1 at ea1252f: one amendment return on AC3 (its clause asked for an integer `id` before and after `id`, which one frame cannot carry; amended at the mini gate, no code change) and one defect return on [O] finding 1 (a factor label whose `NA` is a level passed), findings 2–6 riding the same return as T5 (factor positive control, a vacuous "id" match, the NA-only header, the `id10` hint and probe, `cli.width` pinned in the test helper); 7–8 rejected as style, 9 docs-only counts corrected. Round 2 at 35811e0: [S] blame-history and [S] prior-review clean, all six fixes confirmed; [O] seven findings, none functional — two label columns sharing one name pass (unreachable from `data.frame()` or rsample; rejected as outside the contract), three decorative assertions carried by their siblings (rejected), an `NA` refusal naming the row not the column (rejected, cosmetic), the two style items again (rejected), two noted. Defect-return count 1, amendment-return count 1. The CI watch hit the session ceiling once; merged on the user's re-posed approval at the green head. Nothing graduated or retired.
