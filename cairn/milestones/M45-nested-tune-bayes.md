# M45: The inner loop takes its tuner as an argument, and `nested_tune_bayes()` is its second consumer

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Resolves:** #35 partial
- **Principles touched:** IP2, IP4, GP1, GP2, GP3
- **Branch/PR:** `m045-nested-tune-bayes` · https://github.com/tidymodels/nestedtune/pull/54

## Goal

A user runs nested cross-validation around `tune::tune_bayes()` through
`nested_tune_bayes()`, and the outer loop that runs it is the one
`nested_tune_grid()` runs, told which tuner to call rather than copied.

## Scope

Surface tier: user-facing — a new export and a new attribute on the results
object. Absorbs the ROADMAP candidate row for issue #35 (added 2026-08-31).

**In:** an internal *tuner description* — the tune function to call and the
static arguments only that tuner takes — built by each orchestrator and
threaded through `nested_fold_fit()`, `dispatch_folds()`, the mirai lean
wrapper, `fold_task()` and `final_fit_worker()` in place of the `grid` formal,
the inner call assembled with `rlang::call2()`; `nested_tune_bayes()` with
`iter`, `initial` and `objective` as its own arguments per D-030 (D-040), the
Gaussian-process seed fixed from the fold's tuning seed; entry checks for the
three; a per-fold candidate record that carries `.iter`; a `procedure`
attribute recording what ran, on both orchestrators' results; oracles, RNG and
parallel-identity tests for the new path; docs.

**Out:** a final fit for the Bayesian procedure → M46. finetune's racing and
annealing tuners, and `control_bayes()`'s `no_improve`, `uncertain` and
`time_limit` → ROADMAP candidate row (added 2026-09-01). `initial` as a
`tune_results` object → refused (D-040). A `control` argument or `...`
pass-through → stays closed per D-030/D-038 (issue #33). tune's finalization
of unknown parameter ranges against the inner rset's full frame → ROADMAP
candidate row (added 2026-09-01), an IP1 question on the grid path today.

## Acceptance criteria

- [x] AC1: `nested_tune_bayes(object, resamples, ..., iter = 10, param_info = NULL, metrics = NULL, initial = 5, objective = tune::exp_improve(), event_level = "first", eval_time = NULL)` is exported and returns a `nested_results`; every S3 method `NAMESPACE` registers on that class (`grep 'nested_results)' NAMESPACE`) runs on a `nested_tune_bayes()` result without error, `autoplot()` at each `type`.
- [x] AC2 (live oracle; RB tripwire: no-oracle — the trade accepted at the plan gate, work log 2026-09-01): each completed fold's `.metrics` and `.selected` are identical to a hand-rolled reference loop in `helper-orchestration.R` written from the seed contract (`set.seed(s)`, one `sample.int(.Machine$integer.max, 2 * n)`, kind pinned per fold) that calls `tune::tune_bayes()` on the fold's inner `rset` with the same `iter`, `initial`, `objective` and `control_bayes(seed = <the fold's tuning seed>, allow_par = FALSE)`, then `tune::last_fit()` on the outer split; asserted at `iter = 2` on the deterministic and the stochastic-engine fixtures.
- [x] AC3 (invariant oracle): at `iter = 0`, which `tune:::check_iter()` accepts at tune 2.1.0, `nested_tune_bayes(initial = k, param_info = p)` and `nested_tune_grid(grid = g, param_info = p)` under the same seed give identical `.metrics` and `.selected` in every fold, and each fold's `.grid` is the grid record plus an `.iter` column of `0L`; `g` is `dials::grid_space_filling(p, size = k)` and the fixture's parameter space is integer-only, where that draw does not depend on the seed (a continuous parameter makes it seed-dependent, measured 2026-09-01), so one grid serves every fold through the public function.
- [x] AC4 (IP2): for `nested_tune_bayes()`, the same seed gives an identical result and a different seed different numbers; fold results do not depend on fold order, on the ambient RNG state or on its kind; the caller's RNG state and kind are restored after a completed run, a run with failed folds and an aborted call, and a session with no `.Random.seed` is left with a valid one; a mirai run at two daemon counts above the threshold is `identical()` to the serial run as a whole object. `control_bayes(seed = seeds[[1L]])` is constructed inside the fold's seed scope, and AC2's reference passes the same seed.
- [x] AC5 (IP4): each completed fold's `.grid` holds one row per candidate that fold's `tune_bayes()` scored with an `.iter` column joined from the tuning run's top-level `.iter` (its per-resample `.metrics` frames carry none), and `print()`'s candidates-searched line and `summary()`'s candidate counts count those rows; a result of either orchestrator carries a `procedure` attribute — a named list naming the tuner (`"tune_grid"` or `"tune_bayes"`) and its static arguments (`grid`; or `iter`, `initial`, `objective`; and `param_info`, `event_level`, `eval_time` on both) — that joins `run_attributes()` and so survives every dplyr and vctrs door that set governs; `attr(x, "grid")` and `attr(x, "metrics")` are unchanged on the grid path, and `attr(x, "grid")` is `NULL` on a Bayesian result, documented.
- [x] AC6 (entry checks): `nested_tune_bayes()` refuses before any fitting, naming the user's call, each with its own condition class: an `iter` that is not a single non-negative whole number; an `initial` that is not a single whole number of at least 2, a `tune_results` included; an `objective` not inheriting `acquisition_function`. Every `check_*()` call in `nested_tune_grid()`'s body other than `check_grid()` and `check_grid_params()` is also called in `nested_tune_bayes()`'s body, and a test fires each through it.
- [x] AC7 (the grid path is unchanged): `nested_tune_grid()` on the deterministic and stochastic fixtures under a fixed seed gives `.metrics`, `.selected` and `.grid` `identical()` at `origin/main` and at the branch head, compared at review by running both; the grid-path oracle tests pass with their expectations unedited.
- [x] AC8 (docs): `nested_tune_bayes()`'s help page has a "Differences from calling tune directly" section naming what is settable, the count-only `initial`, and the seed rule (the fold's tuning seed seeds `control_bayes()`, built inside that seed's scope); `nested_tune_grid()`'s help cross-references it; `_pkgdown.yml` lists it under "Running the loop"; `NEWS.md` carries an entry; `cairn/DESIGN.md`'s orchestration family line and architecture paragraph name it and the tuner description.
- [x] AC9: `Rscript -e 'devtools::document()'` produces no diff, `Rscript -e 'devtools::test()'` is clean, and `Rscript -e 'devtools::check()'` reports 0 errors and 0 warnings.

## Coverage

- AC1 → T4, T5
- AC2 → T5
- AC3 → T5
- AC4 → T1, T6
- AC5 → T2, T5
- AC6 → T3
- AC7 → T1, T6
- AC8 → T4
- AC9 → T7

## Tasks

- [x] T1: The tuner description and the call it assembles (RB tripwire: ip-touching — IP2). Internal constructors `tuner_grid(grid)` and `tuner_bayes(iter, initial, objective)` returning a list naming the tune function and its static arguments; `run_tuner()` building the inner call with `rlang::call2()` over `object`, `resamples`, `param_info`, `metrics`, `eval_time`, the tuner's arguments and a control the tuner supplies — `control_grid(allow_par = FALSE, event_level)` or `control_bayes(allow_par = FALSE, event_level, seed = seeds[[1L]])`. `nested_fold_fit()` (`R/nested-tune-grid.R:490`), `dispatch_folds()` and its lean `task` (`R/parallel.R:194-330`), `fold_task()` (`R/parallel.R:1040`) and `final_fit_worker()` (`R/nested-final-fit.R:270`) take `tuner` in place of `grid`; `nested_tune_grid()` and `nested_final_fit()` build `tuner_grid(grid)`. Re-point the mocked worker signatures in `test-parallel-identity.R:348`, `test-parallel-interrupt.R:20,58`, `test-parallel-payload.R:294`, `test-parallel-detection.R:298` and the direct calls in `test-nested-tune-grid-rng.R:80,118`; re-point `helper-time-budget.R`'s ledger for moved lines. Package functions serialize by namespace name, so the description costs nothing on the wire (M12/M23 lesson).
- [x] T2: The record (RB tripwire: ip-touching — IP4). `scored_candidates()` (`R/nested-tune-grid.R:634`) joins `.iter` from the tuning run's rows onto each per-resample frame; `new_nested_results()` (`R/nested-results.R:8`) takes and stamps `procedure`, which joins `run_attributes()` (`R/nested-results.R:259`); `expect_kept()` (`test-dplyr-compat.R:53`) and `expect_record_kept()` (`test-vctrs-compat.R:47`) gain it; `print_candidate_sets()` and `summary()` are run on an `.iter`-bearing `.grid` (M41 lesson: every reader of a changed shape).
- [x] T3: Entry checks in `R/checks.R`: `check_iter()`, `check_initial()`, `check_objective()`, each refusing with a named class and the user's call; a test per refusal asserting the class, and one test per shared `check_*()` firing through `nested_tune_bayes()` (`test-nested-tune-bayes-checks.R`).
- [x] T4: `nested_tune_bayes()` in `R/nested-tune-bayes.R` with roxygen (AC8's section, an engines-guarded example), the `nested_tune_grid()` cross-reference, `_pkgdown.yml`, `NEWS.md`, DESIGN's family line and architecture paragraph; `bayes_param_info()` fixture — a finalized, integer-only parameter set on the deterministic workflow — and a stochastic-engine sibling in `helper-orchestration.R`, built inline where a daemon is involved (M12 lesson).
- [x] T5: Oracles and the readers test (`test-nested-tune-bayes-oracles.R`, provenance header per DESIGN): `reference_nested_bayes_loop()` for AC2 on both fixtures; the AC3 invariant; the AC5 record assertions; the AC1 readers test, its method list taken from `grep 'nested_results)' NAMESPACE` at implementation time and each method called once.
- [x] T6: `test-nested-tune-bayes-rng.R` with AC4's eight properties; a BC10 test in `test-parallel-identity.R` at two daemon counts, its `start_daemons()` waits added as ledger rows in `helper-time-budget.R`; the AC7 before-and-after comparison, its command and result written to the work log.
- [x] T7: `air format .`, `devtools::document()`, `devtools::test()`, `devtools::check()`; NEWS wording derived from the shipped behaviour, not composed.

## Work log

- 2026-09-01: created by /milestone-plan; absorbs the ROADMAP candidate row for issue #35 (added 2026-08-31). Probed by execution (tune 2.1.0): `tune_bayes()` is same-seed identical, its initial stage equals `tune_grid()` on the same space-filling set under the same seed, `iter = 0` is accepted, results carry a top-level `.iter`, `control_bayes()`'s default seed is drawn from the stream, and tune imports GauPro so no dependency is added.
- 2026-09-01: criteria audit ran in full mode (fresh [O] reader, no author): 29 findings over the 16 draft criteria of M45 and M46. Fixed with one clear answer: the reference and the orchestrator both pass an explicit `control_bayes(seed=)`; AC3 restricted to an integer-only fixture; `.iter` joined from the tuning run's rows; `procedure` joins `run_attributes()` and records `param_info`/`event_level`/`eval_time`; the shared-checks promise names its procedure; AC7 became a before-and-after identity of the fixture's numbers; instrument properties moved to T2/T5/T6; the stricter `iter`/`initial` checks got D-040. One finding became a gate question (the one-type oracle on the iteration stage).
- 2026-09-01: plan gate chose `nested_tune_bayes()` as a sibling export over a single function taking the tune function as an argument (topepo's #35 sketch) because D-010's naming convention stands and a sibling needs no superseding entry; falsified by a third tuner arriving whose signature the sibling shape cannot carry without a fourth near-identical export.
- 2026-09-01: plan gate chose `iter`, `initial`, `objective` as own arguments over a `control` argument or adding `no_improve`/`uncertain` because D-030's per-argument rule stands and most `control_bayes()` slots are inert or unreachable inside a daemon; falsified by a caller needing a slot beyond those three to change a number they are shown.
- 2026-09-01: plan gate chose accepting one oracle type on the Bayesian iteration stage (the reference loop, plus serial-versus-parallel identity) over a review brief because the proposals are tune's own search inside D-002's boundary and a brief could not supply an independent oracle for tune's Gaussian process either; the package's own contribution — the call, the seed, the record, the loop — has two types at the initial stage. Falsified by the reference loop and the orchestrator disagreeing for a reason the seeds do not explain, or by a tune release changing `tune_bayes()`'s draw order.
- 2026-09-01: plan chose (step 2, autonomously) one threaded tuner description over three more named formals on the four-function dispatch chain because each prior setting (M34, M35, M41) touched all four; falsified by a tuner whose arguments cannot be captured statically in a call.
- 2026-09-01: T1 done. `R/tuner.R` holds `tuner_grid()`, `tuner_bayes()`, `run_tuner()` (the call via `rlang::call2(.ns = "tune")`, the control built inside it from the fold's tuning seed) and `new_procedure()`; the loop body moved out of `nested_tune_grid()` into `nested_loop()`, which both orchestrators call with their tuner and their own frame as `call`; `nested_fold_fit()`, `dispatch_folds()`, the lean task, `fold_task()` and `final_fit_worker()` take `tuner`; `new_nested_results()` takes `procedure`. Five mocked signatures and four direct calls re-pointed by renaming a formal, so no budgeted line moved. Full suite clean. The question gate was skipped: nothing the plan left open needed the user — the AC3 fixture is a two-integer-parameter workflow (`bayes_workflow()`, landed in the helper with T1) because a single-parameter `grid_space_filling()` draw proved seed-dependent and a two-parameter one comes from sfd's tables with no draw (measured), which is the property AC3 states.
- 2026-09-01: T3 done, committed ahead of T2 because T2's tests call `nested_tune_bayes()`: `check_iter()`, `check_initial()` (a `tune_results` refused with its own bullet) and `check_objective()` in `R/checks.R`, classes `nestedtune_bad_iter`/`_initial`/`_objective`; `R/nested-tune-bayes.R` carries the orchestrator and its help page (T4 finishes the docs); `test-nested-tune-bayes-checks.R` derives the shared-check set from both bodies with `all.names()` and fires each through the new function under a `dispatch_folds()` sentinel.
- 2026-09-01: T2 done. `join_iteration()` stamps each per-resample frame with the row's top-level `.iter` before `scored_candidates_impl()` pools them, and the record orders by `.iter` then `.config` (tune pads `pre*` labels but not `iter*`, so `iter10` would otherwise precede `iter2`); `candidate_key()` drops `.iter` beside `.config`; `procedure` joins `run_attributes()` and `stamp_results()`; `expect_kept()` and `expect_record_kept()` assert it. `test-nested-tune-bayes-results.R` pins the join on hand-built frames and the readers on the shared `bayes_results()` fixture, which every Bayesian test file requests in one spelling so the cache serves it once.
- 2026-09-01: T4 done. Help page with the "Differences from calling tune directly" section, the seed rule and the by-hand recipe, an example on two tunable recipe steps (a single four-level parameter exhausts tune's search at `initial = 2, iter = 2`, printing a backtrace and returning early with no note — measured, and stated on `iter`); `nested_tune_grid()` names its sibling in the description, `@return` and `@seealso`; the five method pages and `agreement()`'s no-method hint name both orchestrators; `_pkgdown.yml`, NEWS, DESIGN's family line and architecture paragraph. Fixtures: `bayes_workflow()` (two `step_ns()` tunables, `df1`/`df2`), `bayes_param_info()` (1..10 each), and `bayes_stoch_param_info()` narrowing ranger's `min_n` to 2..25 — at 40 a candidate predicts a constant, yardstick warns, and the note's backtrace differs between host and daemon, which would fail BC10 on the trace alone; T4's "stochastic-engine sibling" is `stoch_workflow()` under that set. `bayes_results()` spells the shared run once for the cache.
- 2026-09-01: T5 done. `test-nested-tune-bayes-oracles.R`: O1 the reference loop on both fixtures, O2 the `iter = 0` identity with `nested_tune_grid()` on `dials::grid_space_filling(p, size = 3)` (asserted seed-independent in the test, not assumed), the readers test over the NAMESPACE lines the AC1 grep selects plus the four pair methods it skips, the two casts into the class asserting their documented refusal (D-032). Full suite clean. AC5's record assertions sit in `test-nested-tune-bayes-results.R` (T2).
- 2026-09-01: T6 done. `test-nested-tune-bayes-rng.R`: the eight AC4 properties plus a ninth pinning through a mocked `tuner_control()` that the seed handed to `control_bayes()` is the fold's tuning seed and the generator state at that moment is `set_fold_seed()`'s. BC10 appended to `test-parallel-identity.R` (serial vs 2 and 3 daemons, whole object), its `start_daemons()` rowed at `:595` with `times = 2L`. AC7 comparison: `git archive origin/main` extracted to the scratchpad, `pkgload::load_all()` of each tree in its own `Rscript`, each tree's own `helper-orchestration.R` sourced, `nested_tune_grid()` run on the deterministic fixture (`det_grid()`), the continuous one (`grid = 5`) and the stochastic one under seeds 11/20, 11/20 and 12/21, `.metrics`/`.selected`/`.grid` saved and compared with `identical()` — nine of nine TRUE (script: scratchpad `ac7/grid-path.R`, `ac7/compare.R`; result logged here since the scratchpad is session-local).
- 2026-09-01: T7 done. `air format .` changes nothing, `devtools::document()` produces no diff, `devtools::test()` clean (the readers test first read NAMESPACE by a source-tree path that R CMD check's installed copy lacks; it now reads `system.file("NAMESPACE", package = "nestedtune")`), `devtools::check()` 0 errors, 0 warnings, 0 notes. NEWS wording derived from the shipped behaviour and the probes above. Status → review.
- 2026-09-01: review — PR #54 opened as draft; all nine criteria verified with fresh evidence; three-lens review: two lenses clean, the [O] lens 11 findings, 8 fixed at the gate (F1 the inner call built over values, so a tune condition carried the training data in its call; F2–F4 false claims in the help page's not-offered paragraph; F5, F7, F8, F10 comment and test fidelity) and 3 rejected with reasons in the Review section; the F7 edit moved a wait call and its ledger row was re-pointed; `test()` and `check()` clean after the fixes.

## Decisions

## Review

Reviewed 2026-09-01 on PR #54 (draft), branch head 5ac8940, `origin/main` at f547886 (unchanged since the branch was cut, so no merge was needed).

**Evidence per criterion** (fresh runs this review; summaries, not output):

- AC1: `grep 'nested_results)' NAMESPACE` lists 18 methods; `export(nested_tune_bayes)` at NAMESPACE:42; the signature at `R/nested-tune-bayes.R:166-177` matches the criterion formal for formal. The readers test in `test-nested-tune-bayes-oracles.R` reads the same NAMESPACE lines from the installed copy and calls each method once, `autoplot()` at each `type`; passed in the full suite run.
- AC2: `reference_nested_bayes_loop()` (`helper-orchestration.R:131`) draws `sample.int(.Machine$integer.max, 2L * n)` once after `set.seed()`, passes `control_bayes(seed = tuning_seed, allow_par = FALSE)`, and O1 asserts `.metrics`/`.selected` identical at `iter = 2` on the deterministic and stochastic fixtures; passed.
- AC3: the `iter = 0` test (`test-nested-tune-bayes-oracles.R:130`) draws `grid_space_filling(p, size = 3)` twice under different seeds and asserts them identical before asserting fold-by-fold identity with `nested_tune_grid()` and `.iter == 0L`; passed.
- AC4: nine tests in `test-nested-tune-bayes-rng.R` (same seed, different seed, fold order, ambient state and kind, control seed built inside the fold scope, state restored after completion / failed folds / abort, no-`.Random.seed` session) and BC10 in `test-parallel-identity.R:566` (serial vs 2 and 3 daemons, whole object) all passed.
- AC5: `test-nested-tune-bayes-results.R` pins the `.iter` join on hand-built frames and the `procedure` attribute; `run_attributes()` (`R/nested-results.R:274`) lists `procedure`; `expect_kept()`/`expect_record_kept()` assert it across every dplyr and vctrs door; `attr(x, "grid")` set to `NULL` on the Bayesian path (`R/nested-tune-bayes.R:197`) and documented at `:56`; the grid attributes unchanged per AC7's run. All passed.
- AC6: `check_*()` calls in `nested_tune_grid()`'s body: dots_empty, eval_time, event_level, grid, grid_params, metrics, nested, param_info, workflow; in `nested_tune_bayes()`'s body: the same set less grid/grid_params plus iter, initial, objective — read from both bodies with awk this review. Classes `nestedtune_bad_iter`/`_initial`/`_objective` at `R/checks.R:531,551,567,583`; `test-nested-tune-bayes-checks.R` fires each and every shared check through the new function; passed.
- AC7: `origin/main` extracted with `git archive` to the scratchpad; each tree loaded in its own `Rscript` with its own `helper-orchestration.R`; `nested_tune_grid()` run on the deterministic (`det_grid()`, seeds 11/20), continuous (`grid = 5`, seeds 11/20) and stochastic (`stoch_grid()`, seeds 12/21) fixtures; `.metrics`, `.selected`, `.grid` compared with `identical()`: 9 of 9 TRUE; re-run after the gate fixes with `.notes` added, 12 of 12 TRUE. The grid-path oracle files are unedited in the diff except the `grid` → `tuner` formal rename at their mocked signatures.
- AC8: "Differences from calling tune directly" section at `R/nested-tune-bayes.R:104` / `man/nested_tune_bayes.Rd:179` naming the settable arguments, the count-only `initial` and the seed rule; `nested_tune_grid()` cross-references it in its description, `@return` and `@seealso`; `_pkgdown.yml:35` under "Running the loop"; `NEWS.md:3-30` three entries; `cairn/DESIGN.md:66` family line and `:238-244` architecture paragraph name it and the tuner description.
- AC9: `devtools::document()` produced no diff (`git status --porcelain` empty); `devtools::test()` clean, exit 0, no failures; `devtools::check()` 0 errors, 0 warnings, 0 notes (5m 41s). All three re-run after the gate fixes: `document()` regenerates only `man/nested_tune_bayes.Rd` from the edited roxygen, `test()` clean, `check()` 0/0/0 (3m 51s).

**Consistency gate:** `cairn_validate.py` exit 0, all checks pass; advisories only (M45 sizing tripwire at 9 criteria, known from plan; 18 references-staleness lines, pre-existing; no release window). No IP/GP principle changed, so `cairn_impact.py` skipped. Toolchain: `document()` no diff; `pkgdown::check_pkgdown()` no problems; README.Rmd untouched by the diff and README.md the newer of the pair; NEWS entry present with no milestone numbers; `air format --check .` exit 0; `devtools::check()` 0/0/0, so no new top-level file lacks an `.Rbuildignore` entry.

**Independent review** (three fresh-context lenses, none the author): the blame-history lens ([S]) traced the loop extraction, the `grid` → `tuner` rename, the `.iter` join, the ledger row and the checks against D-010/014/016/021/030/036/038/040/041 and found nothing undone or contradicted. The prior-review-record lens ([S]) checked the archived Review findings and every LESSONS line touching the changed code (M07/M14, M12, M16/M31, M35, M41, M42, M44) and found no regression; the GitHub probe found one real inline comment on a workflow file this diff does not touch. The diff-bug lens ([O]) reported 11 findings, ranked; each and its disposition:

- F1 (fixed): `run_tuner()` built the inner call over values, so a condition tune raises carried the workflow and its training data as its call — measured 172,423 characters on a 400-row fixture against 173 for the untouched `last_fit()` call, a regression on the grid path too, which AC7's comparison of `.metrics`/`.selected`/`.grid` could not see. Now built over symbols and evaluated in an environment binding them: 149 characters after the fix, same probe; AC7 re-run 12 of 12 identical.
- F2 (fixed): the help page said every fold runs the full `iter`, but `control_bayes()`'s `no_improve = 10` default was never overridden, so a fold stops after ten iterations without improvement. Prose corrected to state the three kept defaults; `@param iter` and NEWS ("up to `iter` more") corrected with it.
- F3 (fixed, same paragraph): `uncertain` was called a setting that stops the search; it triggers an uncertainty sample and stops nothing.
- F4 (fixed, same paragraph): `save_gp_scoring` was grouped with the settings that print; it writes to a temporary file.
- F5 (fixed): `scored_candidates()`'s totality comment said ordering ran on the key alone; it now names `.iter` as the second, integer column ordered on.
- F6 (rejected): AC7's comparison set could not see F1. The criterion as written was met and is not reinterpreted; F1's own probe is the evidence for what the set missed, and `.notes` joined the re-run.
- F7 (fixed): `test-parallel-payload.R`'s three `.args` stand-ins still carried `grid = 3`; they now carry `tuner = tuner_grid(3)`, the shape `dispatch_folds()` sends.
- F8 (fixed): the AC3 seed-independence assertion compared the ambient state with seed 1; it now compares seeds 1 and 2.
- F9 (rejected): nothing pins `eval_time` reaching `tune_bayes()`. `run_tuner()` forwards it on the one line both tuners share and the grid path's eval-time suite exercises that line; a censored Bayesian fixture is out of this milestone's scope and no criterion asks for it.
- F10 (fixed): a dead `iter` formal on the test helper `candidate_frame()` removed.
- F11 (rejected, pre-existing): `rbind()` of two results keeps the first's `procedure`, as it already kept the first's `grid` and `metrics` on `origin/main`; not introduced here, surfaced at the gate for a Known-issues call.

Gate fixes committed on the branch before the approval marker. The first re-run failed `test-suite-hygiene.R` on the ledger: F7's edit reformatted `test-parallel-payload.R` and moved its `start_daemons()` from line 287 to 295, so the row in `helper-time-budget.R` was re-pointed (the M16/M31 lesson, caught by the guard that exists for it); `document()`, `test()` and `check()` then clean.

