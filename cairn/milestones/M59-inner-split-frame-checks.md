# M59: Every driver refuses a design whose inner splits are not rsplits, do not share the outer fold's frame or its analysis set, or index rows the outer fold holds out

- **Status:** review
- **Priority:** normal
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** IP1, IP2, GP3, GP4
- **Resolves:** —
- **Surface tier:** user-facing — an entry refusal every tuning driver raises, its help page and NEWS
- **Branch/PR:** `m059-inner-split-frame-checks` · https://github.com/tidymodels/nestedtune/pull/69

## Goal

The five orchestrators refuse, at the call the user wrote and before any fold runs, a nested design whose inner splits are not `rsplit`s, whose inner splits do not all carry one frame that is the outer split's own frame or that split's analysis set, or whose whole-frame inner splits index a row the outer split holds out, naming every offending position.

## Scope

**In:** three rules added to `check_nested()` (`R/checks.R`), run after the D-047 rules: every element of each fold's inner `splits` list is an `rsplit`; every inner split of a fold carries one frame `identical()` to the outer split's `$data` or to `rsample::analysis()` of that split (the shape `nested_resamples()` builds and the shape `rsample::nested_cv()` builds); an inner split carrying the outer frame has an `in_id` and a non-`NA` `out_id` inside the outer split's `in_id`. One class (`nestedtune_bad_design`), the driver's call, every offending fold, split and index named, no frame interpolated into a message. The suite's failure fixtures `break_fold()` and `break_inner_split()` rebuilt inside the design's own frame. `@param resamples`, NEWS, the `R/parallel.R` fat-path comment, and a decision entry superseding D-047's consequence clause.

**Out:** the parallel fat path and `is_fold_payload()`'s shared-frame clause stay as the lean gate the dispatch tests drive with stand-in payloads (gate choice, 2026-09-04) — removing them is a candidate row only if a later milestone wants the deletion. Index range against the frame (an index past the frame's end, a negative index) is left to `rsample::analysis()` and `last_fit()`, which refuse at the fold as `test-nested-tune-grid-failures.R` asserts, and an analysis-framed inner split (`nested_cv()`) gets no index rule for the same reason. Two label columns sharing a name, and every other shape M55 left out, stay in the rows M55 named.

## Acceptance criteria

- [x] AC1: Each of the five drivers (`nested_tune_grid()`, `nested_tune_bayes()`, `nested_tune_race_anova()`, `nested_tune_race_win_loss()`, `nested_tune_sim_anneal()`) refuses at entry, before any fold runs, with condition class `nestedtune_bad_design` and `conditionCall()` naming the driver, a design in which an element of some fold's inner `splits` list is not an `rsplit`; the message names every offending fold and split position. Shown by tests planting the defect as a string, as a bare list and as an `rset`, at the first, the last and all three outer folds of `det_nested()`, and at the first and the last inner split.
- [x] AC2: Under the same class and call, each driver refuses a design in which a fold's inner splits do not all carry one frame that is `identical()` to the outer split's `$data` or to `rsample::analysis()` of that split; the message names every offending fold and, for a fold whose splits disagree among themselves, every split. Shown by tests planting, at the first, the last and all three outer folds of `det_nested()`: every inner split on a foreign frame; one inner split on a foreign frame; the outer split replaced by one over a foreign frame.
- [x] AC3: Under the same class and call, each driver refuses a design in which an inner split carrying the outer split's own frame has an `in_id`, or a non-`NA` `out_id`, holding a row index outside the outer split's `in_id`; the message names every offending fold, split and index. Shown by tests planting an outer-assessment row and an index past the frame's end, each into `in_id`, into `out_id`, and into both, at the first, the last and all three outer folds of `det_nested()`.
- [x] AC4: `check_nested()` returns its argument invisibly for each of these well-formed designs: a `nested_resamples()` design; an `rsample::nested_cv()` design built on a data.frame and one built on a tibble; an `rsample::nested_cv()` design whose outer `rset` is an evaluated `manual_rset()` with a repeated row; `det_nested()` and `repeated_design()`. And `nested_tune_grid()` completes every fold (`.completed` all `TRUE`) on the data.frame and the tibble `nested_cv()` designs, serially.
- [x] AC5: `@param resamples` on `nested_tune_grid()` (inherited by the four sibling pages) states the three rules and their refusal class, and one `NEWS.md` entry states them.
- [x] AC6: `devtools::document()` produces no diff; `devtools::test()` is clean with no new skips; `devtools::check()` reports 0 errors, 0 warnings, 0 notes.

## Coverage

- AC1 → T1, T2
- AC2 → T1, T2
- AC3 → T1, T2
- AC4 → T1, T2
- AC5 → T4
- AC6 → T3, T5

## Tasks

- [x] T1: Tests first. Extend `malformed_designs()` (`tests/testthat/helper-orchestration.R:1991`) with the AC1–AC3 plantings — each recording the folds, splits and indices the refusal must name — and add the AC4 controls beside M55's; the grid-driver block in `test-nested-tune-grid-checks.R` asserting class, call and every planted position, one block per other driver in its checks file; the AC4 completion runs. Red.
- [x] T2: `check_inner_splits()` in `R/checks.R`, called last in `check_nested()` (after `check_label_values()`, `:206`): the `rsplit` rule; the one-frame rule, `identical()` against `split$data` first (a pointer fast path for `nested_resamples()` designs) and against `rsample::analysis(split)` only when the outer split's indices lie in its frame, since M54 leaves an out-of-range outer index to `last_fit()`; the containment rule for whole-frame splits over `in_id` and non-`NA` `out_id`; one class, the driver's call, every position; no frame in any message (the M05/M45 lesson). Green.
- [x] T3: Re-vehicle the fixtures on the 2026-09-04 probe: `break_fold(stage = "inner tuning")` empties every inner split's `in_id` (the fold fails at inner tuning with tune's "All models failed" note), `break_fold(stage = "outer fit")` appends an index past the frame's end to the outer split's `in_id` (tuning completes, `last_fit()` refuses, as the appended case at `test-nested-tune-grid-failures.R:725` already shows), `break_inner_split()` empties one inner split's `in_id` (the fold completes on a truncated design). Replace the three assertions naming the recipe's foreign-variable message (`test-nested-tune-grid-failures.R:65,77,281`) with tune's new note text; run the 13 files `grep -l 'break_fold\|break_inner_split' tests/testthat/test-*.R` lists.
- [x] T4: `@param resamples` (`R/nested-tune-grid.R:36-50`), the NEWS entry, the `is_fold_payload()` comment (`R/parallel.R:108-118`, which also still says the check asks only for the class and a row) rewritten to say the gate now serves stand-in payloads and defence in depth; `devtools::document()`; append the decision entry superseding D-047's consequence clause, above the template block in `cairn/DECISIONS.md` (the M33 lesson).
- [x] T5: Full `devtools::test()`, `devtools::check()`, `air format --check` on every touched file before the review push (the M56 lesson).

## Work log

- 2026-09-04: created by /milestone-plan, promoting the candidate row "Refuse an inner `rset` built over a frame other than its outer split's, and inner splits that are not `rsplit`s" (M19 Out, M38 Out, M55 Out) at the user's choice, ahead of the row's stated promotion condition. Probes this day: a hand-built inner split whose `in_id` holds an outer-assessment row runs to completion unrefused; a `nested_cv()` inner frame is `identical()` to `rsample::analysis()` of its outer split on a data.frame and on a tibble; the identity check costs 0.38 s for ten folds of a 1e6 × 11 `nested_cv()` design; emptying inner or outer `in_id`s reproduces the three fixture outcomes inside the design's own frame.
- 2026-09-04: criteria audit ran in full mode ([O] fresh reader): eight findings. Fixed — the drafted "outer fit" fixture (an emptied outer `in_id`) would have tripped AC3's own containment rule, replaced by an appended out-of-range outer index; the fixture-helper criterion and the source-comment clause bound instrument state and moved to T3 and T4; AC1's plantings gained form variety (string, bare list, `rset`); AC3's gained an index past the frame's end; the test-local `valid_folds()` left AC4's controls and a driver-level completion run joined them; "14 files" corrected to 13. Judgments — the fat path's purpose and D-047's clause went to the gate; an analysis-framed inner split's index range stays Out.
- 2026-09-04: plan gate chose including the index containment rule over a candidate row because a whole-frame inner split reaching an outer-assessment row is the IP1 breach in check form and runs unrefused today; falsified by an rsample-built design the rule refuses.
- 2026-09-04: plan gate chose `identical()` against the outer frame and its `analysis()` over a row-count-and-names shape check because a same-shape wrong frame is exactly the case the parallel fat path exists for; falsified by the entry cost dominating a real run, which the 0.38 s per ten folds at 1e6 rows argues against.
- 2026-09-04: plan gate chose rewriting `break_fold()` and `break_inner_split()` over rewriting their 43 call sites because the helpers own the vehicle and the call sites assert outcomes; falsified by a call site that depended on the foreign frame itself rather than on the stage that fails.
- 2026-09-04: plan gate chose superseding D-047's consequence clause while keeping the fat path over deleting the fat path because the dispatch tests drive `dispatch_folds()` with stand-in payloads that need the gate, and the clause stays as defence in depth; falsified by the fat path taking a real design after the check ships, which would mean the check has a hole.
- 2026-09-04: T1 — `malformed_designs()` gained 51 inner-split plantings (18 non-rsplit forms × positions, 15 frame plantings, 18 index plantings) each carrying the exact `fragments` its refusal must contain; the grid checks file asserts them, adds the tibble and repeated-row `nested_cv()` controls and the two serial completion runs. Red: every new planting reaches the dispatch sentinel unrefused.
- 2026-09-04: checkpoint, unverified — T2's `check_inner_splits()` (three rules, last in `check_nested()`), T3's re-vehicled fixtures (emptied inner `in_id`s, an appended out-of-range outer index; the three note assertions now read `a dimension is zero` and `past the end`) and T4's `@param`, NEWS, `R/parallel.R` comment and D-049 are written and air-clean; the grid checks file is green; the full suite is running and `document()` has not run, so no task is ticked yet.
- 2026-09-04: full suite under the checkpoint: two failures, both instrument-side. `test-parallel-identity.R` BC6 compared a daemon's `i` against the host's `ℹ` in tune's new zero-row note (the daemon has no UTF-8 console), so its `unwrap()` now reads both spellings as one; the `last_fit()` test's substituted outer `in_id` `c(1L, 999999L)` is now refused at entry, since the fold's inner splits index rows that split does not hold, so it stays as the refused control and the appended index is the loop vehicle. Both files green on rerun.
- 2026-09-04: `devtools::check()` under the checkpoint: 0 warnings, 0 notes, one test failure — the BC6 comment insertion moved ten `start_daemons()` call sites in `test-parallel-identity.R` by six lines and the `helper-time-budget.R` ledger cites them by `file:line` (the M16 lesson); ten rows renumbered, `test-suite-hygiene.R` green.
- 2026-09-04: T2–T5 ticked on the second `devtools::check()`: 0 errors, 0 warnings, 0 notes, 11m 39s; `document()` no diff; `air format --check` clean on every touched file. Status → review.
- 2026-09-04: review step 2–3 checkpoint — PR #69 opened as draft; `document()` no diff; the five driver checks files green; AC1–AC5 evidence recorded and ticked; `devtools::check()`, the full `devtools::test()` for AC6 and the three review lenses still running.
- 2026-09-04: review steps 3–6 — AC6 evidence recorded (full suite exit 0, 0 skips; `check()` 0/0/0), gate green, three lenses reported 17 findings; ledger and proposed dispositions in the Review section, pending the maintainer's triage at the gate.
- 2026-09-04: gate triage accepted fix-now for O1, O2/O10, O3, O4, O8, O11, O12 — `check_inner_splits()` leaves a fold whose analysis set cannot be built to `last_fit()`, names every split of a disagreeing fold with what it carries (other frame, analysis set, outer frame), and deep-compares once per distinct frame; the two comments corrected; a mixed-frame planting (3 records, 83 total), an out-of-range outer control on a `nested_cv()` design, a no-column-name assertion and the floor at 70 added; checks and payload files green, air clean; full suite and `check()` re-running.
- 2026-09-04: `check()` after the fix-now commit: one failure, the M16 ledger again — the O4 comment grew `test-parallel-payload.R` by two lines, moving its `start_daemons()` call from 302 to 304; row renumbered, `test-suite-hygiene.R` green; full `devtools::test()` before the renumber was exit 0 with no skips; `check()` re-running.
- 2026-09-04: review re-verification after the fix-now commits green (`check()` 0/0/0, full suite 0 skips); dispositions recorded; merge approval requested at the gate.
- 2026-09-04: step-7 approval: PR #69 approved for merge.

## Decisions

## Review

_Reviewed 2026-09-04 on PR #69 (`m059-inner-split-frame-checks`, six commits ahead of `origin/main`, which had not moved since the branch was cut)._

- AC1: `devtools::test(filter = "checks")` green across the five driver checks files (grid, bayes, race, sim-anneal, plus final-fit). `malformed_designs()` yields 80 plantings; 18 are the non-`rsplit` forms (string, bare list, `rset`) × outer positions (first, last, all three) × inner positions (first, last). The grid file asserts class `nestedtune_bad_design`, `conditionCall()` naming `nested_tune_grid`, no sentinel reached, and every planted `Outer fold f: inner split s` fragment; the bayes, both race and sim-anneal blocks assert class and call on every planting.
- AC2: same run; 15 frame plantings (every inner split on the foreign frame, every inner split on a same-shape frame, one inner split on the foreign frame at first and last, the outer split replaced by a foreign-frame split) at first, last and all three folds. The fragment assertions hold the message to the fold, and to the disagreeing split alone where one split is foreign (`inner split 1 carries`, no `inner split 2`).
- AC3: same run; 18 index plantings (an outer-assessment row and 999999, into `in_id`, `out_id` and both) at first, last and all three folds; each message asserted to carry `Outer fold f, inner split s` and `holds <index>`, the slot named being the planted one.
- AC4: same run; `check_nested()` returns each of the eight well-formed designs invisibly and identical (`det_nested()`, factor labels, `valid_folds()`, `repeated_design()`, `nested_resamples()`, `nested_cv()` on a data.frame, on a tibble, and with an evaluated `manual_rset()` outer that repeats a row), and `nested_tune_grid()` completes both folds (`.completed` all `TRUE`) on the data.frame and tibble `nested_cv()` designs serially.
- AC5: `@param resamples` in `R/nested-tune-grid.R` states the three rules and the `nestedtune_bad_design` class; the four generated pages (`nested_tune_grid`, `nested_tune_bayes`, `nested_tune_race`, `nested_tune_sim_anneal`) carry the text; `NEWS.md` opens with one entry stating the three shapes and the class.
- AC6: `devtools::document()` no diff; full `devtools::test()` exit 0 across 58 files, no failure, warning or skip (M58's baseline: 0 skips); `devtools::check()` 0 errors, 0 warnings, 0 notes, 10m 59s.

**Consistency gate.** `cairn_validate.py` passed (18 references-staleness advisories, pre-existing); no `DESIGN.md` principle changed, so `cairn_impact.py` skipped. Toolchain: `document()` no diff; `pkgdown::check_pkgdown()` no problems; NEWS carries the entry; README.Rmd and README.md untouched on the branch (same last commit); no new top-level file; `check()` 0/0/0.

**Independent review** (three lenses, fresh context): [O] diff-bug 15 findings, [S] blame-history 1, [S] prior-review 1 (the same BC6 point) and a note that the M23 review F1 finding is closed by this branch; the prior-review probe found one real inline comment repo-wide (topepo, PR #30, unrelated). Dispositions proposed at the gate, recorded on decision:
- O1 (`R/checks.R:568-582`): a `nested_cv()` design whose outer `in_id` reaches past its frame is refused by the frame rule with a false message, since `outer_analysis()` returns `NULL` and no inner split is admissible; the Scope leaves that index to `last_fit()`. Reproduced. Proposed fix now: when the analysis set cannot be built, the fold's non-whole splits are left to `last_fit()`, with a control asserting the design is admitted at entry.
- O2/O10 (`R/checks.R:464-472`): the frame rule anchors on split 1, so a fold whose first split carries the analysis set and whose others carry the outer frame names splits 2 and 3; and AC2 says a disagreeing fold names every split, where the code and the T1 test name only the disagreeing ones. Reproduced. Proposed fix now: a disagreeing fold names every split with what it carries. The maintainer decides whether AC2 read literally was failing (a floor return) or the fix lands at the gate.
- O3 (`R/parallel.R:113-116`): the rewritten comment says the stand-in payloads fail the shared-frame clause, but they fail the first clause (no `split`/`inner` names). Verified. Proposed fix now: comment corrected to defence in depth alone.
- O4 (`tests/testthat/test-parallel-payload.R:113-117`): a comment still says `check_nested()` does not enforce the one-frame invariant. Verified. Proposed fix now.
- O8 (`R/checks.R:449-460`): the analysis-set compare runs once per inner split though they share one frame object; measured 0.31 s for 5 × 10 splits at 3e5 rows. Proposed fix now: compare once per distinct frame.
- O11 (`test-nested-tune-grid-checks.R:748-753`): the width bound is a loose proxy for "no frame in a message". Proposed fix now: also assert no message carries a column name of the fixture data.
- O12 (`expect_gt(length(planted), 20L)` in the five checks files): a floor sized for 29 records now guards 80. Proposed fix now: raise to 70.
- O5, O6, O7, O9, O15: a non-numeric index slot coerces with a warning and reports `NA`; a fractional index truncates and passes; an element-wise `NA` in `out_id` is dropped; a non-data.frame outer `$data` reports as a frame mismatch; an empty inner `in_id` passes containment (the fixture vehicle). Proposed follow-up: one candidate row for index-slot shapes the rules leave to rsample.
- O13, S1, P1 (`test-parallel-identity.R:255-259`): the BC6 glyph normalization covers `ℹ` only and widens BC6's exemption without a decision entry. Proposed follow-up: normalize at the daemon (`cli.unicode`) so BC6 compares identical text; candidate row.
- S count: the fixture helpers have 14 call-site files, not the 13 T3 states; the sweep ran the files `grep -l` listed, so the count is a record slip, not a missed file. Noted.
- O14: the re-vehicled inner-tuning note `a dimension is zero` is R's own `La.svd` text, not a platform LAPACK message, and the fixture's dependence on the PCA step is what `det_workflow()` fixes. Proposed reject.
**Re-verification after the fix-now commits** (`cf7648f`, `2274d46`): `devtools::test(filter = "checks|parallel-payload")` green; `malformed_designs()` now 83 plantings (18 non-rsplit, 18 frame including the 3 mixed-frame records, 18 index, 29 M55); full `devtools::test()` exit 0, 58 files, no failure or skip; `test-suite-hygiene.R` green after the ledger renumber; `devtools::check()` 0 errors, 0 warnings, 0 notes, 5m 6s; `document()` no diff; air clean on every touched file. AC1–AC3 and AC6 evidence above stands on these runs.

**Dispositions (gate, 2026-09-04):** fix now — O1, O2/O10, O3, O4, O8, O11, O12, landed as above (O2/O10 taken as a fix at the gate, not a floor return: the criterion's "every split" is now what the message does). Follow-up — O5, O6, O7, O9, O15 as one candidate row (index-slot shapes the rules leave to rsample); O13/S1/P1 as one candidate row (normalize cli glyphs at the daemon so BC6 compares identical text). Rejected — O14 (R's own `La.svd` text, not a platform message). Noted — the 14-file count. Conversation: PR #69 — empty (no reviews, comments or threads).


